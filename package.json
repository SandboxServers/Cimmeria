{
  "name": "cimmeria-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Repo-level tooling for Cimmeria. Currently just markdown lint; not the runtime server (that lives under crates/).",
  "scripts": {
    "lint:md": "markdownlint-cli2",
    "lint:md:fix": "markdownlint-cli2 --fix"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.17.0"
  }
}
