[package]
name = "cimmeria-entity"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Entity system runtime for the Cimmeria server emulator"

[dependencies]
cimmeria-common = { workspace = true }
cimmeria-mercury = { workspace = true }
cimmeria-defs = { workspace = true }
cimmeria-commands = { workspace = true }
tokio = { workspace = true }
bytes = { workspace = true }
serde = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }

[build-dependencies]
cc = "1"

[dev-dependencies]
# Lightweight tracing-layer-based capture for negative-log regression
# guards. Pulls in tracing-subscriber as a dev-only dependency; not
# bundled into the production binary.
tracing-subscriber = { workspace = true }
