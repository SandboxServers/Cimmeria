//! SGWInventoryManager interface exposed CellMethods (indices 36–42) and the
//! `onEntityProperty` payload the inventory paths send.
//!
//! The handlers are in `cimmeria_services::cell::cell_methods::inventory`.

pub mod constants;

pub use constants::*;
