# H09 Worknotes

> Type: reference. Audience: Harset rebuild coordinator.
> Companions: [README.md](../README.md), [work-packets.md](../work-packets.md), [H11.md](H11.md).

## Contract

- **Packet:** H09 — multi-ability NPC ability sets. Widen the
  `ability_set_abilities` primary key to `(ability_set_id, ability_id)`,
  confirm the loader and the three-bucket chooser handle a multi-row set, and
  give ability sets 4 (Jaffa staff) and 5 (Goa'uld ribbon device) their real
  membership.
- **Base:** `content/harset-wave2` @ `94e65324b159e9cdc49a3fd131fac2af99640f77`
  ("chore(harset): register the three faction chain seed files in
  database.sql").
- **Branch:** `harset/H09`.
- **Worktree:** `C:\Users\Steve\source\projects\Cimmeria\.claude\worktrees\harset-H09`.
- **Depends:** H11 (merged in wave 1 — allocated sets 4 and 5 with one ability
  each, and opened this packet as its integration request 2).
- **Advisors:** `combat-systems-advisor` (set composition + chooser),
  `database-persistence` (key change + seed shape),
  `testing-validation-engineer` (post-diff review). One round each; findings
  and resolutions below.
- **Excluded by the packet:** new ability design, per-template overrides,
  changing the chooser's bucket logic beyond multi-row support. All three held.

## Headline finding: all three abilities the packet named are unusable

The packet text says to give set 4 "1482 Ground Blast, 1768 Double Blast
alongside 594 Strike". **None of the three qualifies**, and the reason is
structural rather than per-ability.

H11 established the membership rule: an ability needs a non-NULL
`event_set_id`, because that column gates the entire Ability_Begin /
Ability_End `onSequence` broadcast
(`crates/services/src/cell/abilities/use_ability/handle.rs:524`). An ability
without one deals damage and plays no animation at all.

| id | name | `event_set_id` | `is_ranged` | `max_range` | `items_event_sets` bindings | verdict |
|---|---|---|---|---|---|---|
| 594 | Strike | **NULL** | f | 0 | **0** | rejected — no animation; also generic melee bound to no weapon |
| 1482 | Ground Blast | **NULL** | t | **3000** | **0** | rejected — no animation; `max_range = 3000` also poisons `ability_ranges`, so the NPC stops closing distance on whatever tick it is selected |
| 1768 | Double Blast | **NULL** | t | 0 | **0** | rejected — no animation |
| 479 | Staff Blast | NULL | t | 0 | 0 | rejected — no animation |
| 540 | Staff Strike | NULL | f | 0 | 0 | rejected — no animation |

The structural part: **only 35 of the 1,886 rows in `resources.abilities`
have a non-NULL `event_set_id`, and every one of the 35 is an auto-attack or a
melee auto-attack.** There is no animatable "special" staff or ribbon ability
anywhere in the seed. A variety set built out of Ground Blast / Double Blast /
Staff Blast is not something this packet declined to build — it is not
expressible in the shipped data.

What the data *does* express is a pair. `resources.items_event_sets` binds
every weapon to exactly two abilities, `EVENT_ITEM_RANGED = 7` and
`EVENT_ITEM_MELEE = 6`:

- **staff** — 204 distinct staff items, all binding 584 (ranged) + 710 (melee)
- **ribbon device** — 151 distinct items, all binding 712 (ranged) + 711 (melee)

So the honest full list for each set is two rows, not three. The three-row
acceptance case is exercised by a test-local sentinel set instead; see
[Guards](#guards).

## Final set membership

| Set | Members (ascending id — the order the loader returns and the chooser walks) | Qualification of each |
|---|---|---|
| 4 Jaffa staff | **584** Staff Auto Attack, **710** Staff Melee AA | 584: `event_set_id = 3`, `max_range = 0` → falls back to `NPC_ATTACK_RANGE` (30), `required_ammo = 0`, cooldown 3, bound to `EVENT_ITEM_RANGED` on all 204 staff items. 710: `event_set_id = 300`, `max_range = 0`, `required_ammo = 0`, cooldown 2, the `EVENT_ITEM_MELEE` half of the same 204 bindings. |
| 5 Goa'uld ribbon device | **711** Ribbon Device Melee AA, **712** Ribbon Device Auto Attack | 711: `event_set_id = 300`, `max_range = 0`, `required_ammo = 0`, cooldown 2, `EVENT_ITEM_MELEE` on all 151 ribbon items. 712: `event_set_id = 300`, cooldown 2.5, the `EVENT_ITEM_RANGED` half. |

H11's single rows (584 on set 4, 712 on set 5) are kept as entries, per the
packet. Sets 1, 2 and 3 are untouched and still hold one ability each.

`ability_sets.description` is unchanged on both rows — "Jaffa staff ability
set" and "Goa'uld ribbon device ability set" are still accurate.

### Three consequences worth stating plainly

**1. Melee at 30m — FIXED in session 2, not accepted.** `ability_ranges`
(`cell/service/npc_ai/ability_select.rs`) returned `NPC_ATTACK_RANGE` (30)
whenever the def's `max_range` was the `0` sentinel, which is true for every
ability in the seed except 1482 (3000) and 1949 (2500), and it did not
distinguish melee from ranged. So a Jaffa that picked 710 on a tick played a
staff-swing animation at up to 30 metres.

Session 1 accepted that on the grounds that the python selector never
range-gated ability choice either — `deprecated/python/cell/SGWMob.py`
`classifyHostileAbility` filters on hostile type, `TCM_Single`, cooldown and
ammo, and then carries the literal comment `# TODO: Check distance, LOS`
(line 227) before returning `ABILITY_Usable` — and that fixing the range
model was a chooser change outside the packet.

**That reasoning does not survive two facts the coordinator raised on
hand-off.** H13 made fifteen Harset spawn rows `is_stationary`, and a live
query says **twelve of the fifteen use ability set 4**: a pinned sentry can
never close distance, so it alternates staff shot and staff swing forever,
and half its attacks are an animation that contradicts what it is doing. The
2026-09-18 Castle playtest
([`docs/analysis/playtests/2026-09-18-colo-castle/`](../../playtests/2026-09-18-colo-castle/README.md))
recorded testers reporting exactly this class of thing as "broken AI".
Second, the python TODO is not a licence to skip the gate — it *is* the
original developers' evident intent, which the owner's standing preference
is to finish where doing so is server-authoritative and client-compatible.

So the scope was extended by the smallest correct amount: the selector may
not pick an `is_ranged = false` ability for a target outside melee reach. See
[The melee reach gate](#the-melee-reach-gate) below. Set 5 was the sharper
case than the stationary Jaffa: 711 sorts below 712, so the melee half was
the ribbon NPC's *primary* at any distance.

**2. Attack rate roughly doubles for sets 4 and 5.** There is no GCD — only
the per-ability cooldown is started (`handle.rs:399-406`) — and the AI tick
runs every ~2s. One 3s ability fires on alternate ticks (~1 attack / 4s); 584
(3s) + 710 (2s) leaves something off-cooldown nearly every tick (~1 attack /
2s). Same for the ribbon pair. The 1x rate was an artefact of the primary-key
bug, not a tuning decision anyone made, so this is accepted rather than
compensated for. Flagged for M1 UAT.

**3. Set 5 is melee-first — now range-dependent.** `choose_npc_ability` sorts
ascending and takes the first off-cooldown id, so 711 (melee) was the ribbon
NPC's *primary* at every distance and 712 (ranged) the fallback — the inverse
of set 4. This is cosmetically inert (711 and 712 share `event_set_id = 300`,
so they emit the same `onSequence`), but 711 is `is_ranged = false`, so most
ribbon attacks rolled against melee rather than ranged defence in
`calculate_qr` (`abilities/damage_apply/mod.rs:92-93`).

After the reach gate the ordering still holds *inside* 3 metres, which is
correct — a ribbon device at arm's length should swing — and 712 wins at every
distance beyond it. The ordering is no longer a defect; it is the tie-break
that decides which half wins when both are usable.

## The melee reach gate

Added in session 2, on top of the multi-row support the packet asked for.
Three files under `crates/services/src/cell/`.

### The constant

`NPC_MELEE_RANGE: f32 = 3.0` in `combat/threat/aggro.rs`, next to
`NPC_ATTACK_RANGE`. Derived from the weapon table, not invented:

| Source | Value |
|---|---|
| `resources.items.max_melee_range`, across all 1,299 items binding an `EVENT_ITEM_MELEE` (`event_id = 6`) ability | `0` (5 items), `2` (1,116), `3` (178) |
| The 204 Jaffa staff items (those binding 710) | `3` (177), `2` (27) |
| The 151 ribbon-device items (binding 711) | `2` (150), `0` (1) |
| `resources.items.max_ranged_range` on those same 1,299 items | `30` (1,298), `35` (1) |

3 is the largest melee reach any shipped weapon expresses, so the gate can
never truncate a real weapon, and it is the value 177 of the 204 staff items
carry — the family the gate was added for. The last row is the corroboration
that made this the right shape: `NPC_ATTACK_RANGE = 30.0` already *is* the
weapon table's `max_ranged_range`, so both constants now come from the same
two columns of the same table.

Per-weapon reach would be strictly better, but an NPC has no resolvable
weapon item — `entity_templates.weapon_item_id` has zero consumers under
`crates/` and the Goa'uld templates carry no `WP-*` component at all. The
ceiling of the observed range is the honest approximation until that changes,
and swapping it out later needs no call-site change because every reader goes
through `effective_max_range`.

Client evidence for the same pair of columns:
[`findings/combat-formulas-client-evidence.md`](../../../reverse-engineering/findings/combat-formulas-client-evidence.md)
E4 recovers `MeleeRanges` / `RangeRanges` from the cooked item schema, with
"item 21 SGHC 6 SMG range 2-30, melee 0-2".

### Where the gate lives

Both options the coordinator offered turned out to be needed, for different
requirements, so the change is layered rather than either/or:

- **`effective_max_range(def, npc_attack_range)`** — new private helper in
  `npc_ai/ability_select.rs` that resolves the `max_range` sentinel through
  `is_ranged`. `ability_ranges` now calls it, so a chosen melee ability
  reports a 3 m `max_range` to the fight tick and the existing out-of-range
  arm handles the movement for free. A **missing def** still resolves to
  `npc_attack_range`, never the melee reach — an unknown ability must not be
  silently confined to 3 m.
- **`choose_npc_ability_within_reach(npc_id, space_mgr, target_dist,
  npc_attack_range)`** — the new production entry point. Prefers the
  lowest-id off-cooldown ability that is usable at `target_dist`; when none
  is, delegates to the unfiltered `choose_npc_ability`. `fight.rs` now
  computes `dist_to_target` before the pick and calls this.

`choose_npc_ability` is untouched and stays production-reachable as the
fallback arm, so the three-bucket partition is intact and the existing
selector tests keep exercising the real code path with no edits.

**Why `is_ranged = false` wins even over a non-zero `max_range`:** 148 melee
rows in `resources.abilities` carry a `max_range` between 100 and 2500,
plainly not the metres the fight tick measures in. Honouring those would
reproduce this defect in a worse form. None of the four abilities in sets 4
and 5 is affected — all carry the `0` sentinel.

**Why the fallback rather than `None`:** `None` is the caller's "all cooling,
hold fire" signal. Returning it when nothing is in reach would freeze a
melee-only NPC at distance instead of walking it in. This is coordinator
requirement (c), and it has its own revert-verified guard.

### Behaviour, by the four required cases

| Case | Before | After |
|---|---|---|
| Set 5 (711 melee, 712 ranged), target at 20 m | picks 711, swings at air | picks 712, fires |
| Set 5, target at 2 m | picks 711 | picks 711 — the gate is a distance filter, not a preference for ranged |
| Mobile NPC, only melee off cooldown, target at 20 m | fires at 20 m | takes the movement arm, then swings once inside 3 m |
| Stationary NPC, only melee off cooldown, target at 20 m | fires at 20 m forever | `stationary_holds` (which since H51 also turns it to face the target) |
| Stationary NPC with a ranged half available | 50/50 by cooldown phase (set 4) or always melee (set 5) | always the ranged half |
| Sets 1, 2, 3 and every Castle set | — | **provably identical.** All hold a single `is_ranged = true` ability, so the filter either accepts the same pick the unfiltered walk makes or accepts nothing and delegates to it. Sets 4 and 5 are the only sets in the seed carrying a melee ability, and the pre-existing `npc_ai` selector, range and facing tests pass unmodified. |

## What changed

### Schema — the packet names the wrong file

The packet's entry list and owned-paths list both name
`db/resources/Abilities/Tables/ability_set_abilities.sql` as the file holding
`PRIMARY KEY (ability_set_id)`. **It does not.** That file is a bare
`CREATE TABLE` with two `NOT NULL` columns and no constraint of any kind.
Every primary key in the resources schema lives in
`db/resources/_primary_keys.sql`, and inlining this one would have been the
only deviation in the tree.

The widen therefore landed in `db/resources/_primary_keys.sql`. The `Tables/`
file gets a six-line comment pointing at where the key actually lives, so the
next reader who follows the packet's owned-path list to that file does not
find an empty hole. Recorded as a correction to the packet text.

`db/database.sql` ordering, verified: DDL at line 190, `_primary_keys.sql` at
264, the seed at 273, `_foreign_keys.sql` at 348. Primary keys before seed is
exactly why a second row aborted a fresh load.

### Nothing references the key

Both foreign keys naming `ability_set_abilities` are **outbound** — it is the
child in both (`ability_id → abilities`, `ability_set_id → ability_sets`,
`_foreign_keys.sql:29,37`), and `entity_templates_ability_set_id_fkey`
already targets `ability_sets`, the parent table. So `DROP CONSTRAINT` is not
blocked and nothing cascades. No additional index was added: `ability_set_id`
is the leading column of the composite key, so the three production
subqueries' `WHERE asa.ability_set_id = …` is a prefix scan and `ability_id`
as the second column makes the index covering and already in
`ORDER BY ability_id` order.

### Rust — no chooser change was needed *for multi-row support*

> Session 2 added one anyway, for a different reason: the melee reach gate
> above. Everything in this subsection is still accurate about multi-row
> support itself — the loaders and the bucket walk needed nothing.

Both advisors independently confirmed the Rust side was already multi-row
ready, which is what H11's integration request 2 predicted:

- All three ability-set loaders use the same correlated
  `array_agg(asa.ability_id ORDER BY asa.ability_id)` subquery —
  `cell/spawner/npcs.rs:151`, `cell/spawner/templates.rs:92`,
  `base/gm_spawn.rs:183`.
- `spawn_npc_from_record_into` (`cell/space_manager/spawn.rs:241-249`) already
  loops `record.ability_ids` and only falls back to `NPC_DEFAULT_ABILITY` when
  the bucket is empty.
- `choose_npc_ability` (`cell/service/npc_ai/ability_select.rs`) sorts the
  known ids and `find`s the first off-cooldown one — it walks N.

So the only Rust edits are **visibility widenings plus documentation**, no
behaviour change:

| File | Change |
|---|---|
| `cell/service/npc_ai/ability_select.rs` | `choose_npc_ability` from `pub(in crate::cell::service)` to `pub(in crate::cell)`; added a `# Multi-ability sets` doc section explaining that ascending-id order makes the lowest id the primary and the rest cooldown fallbacks. |
| `cell/service/npc_ai/mod.rs` | the `#[cfg(test)]` re-export from `pub(super)` to `pub(in crate::cell)`. |
| `cell/service/mod.rs` | `mod npc_ai;` → `pub(in crate::cell) mod npc_ai;`. **Outside the packet's owned paths** — see [Integration requests](#integration-requests). |

All three exist so the live-DB guard in `cell/spawner/tests/harset/` can name
`choose_npc_ability`. `npc_ai` is a private module inside a private `service`
module, so the spawner tests — which are descendants of `crate::cell` but not
of `crate::cell::service` — could not reach it. Nothing outside `crate::cell`
gains visibility, and the re-export stays `#[cfg(test)]`.

### Files touched

Modified:

- `db/resources/_primary_keys.sql` — the widen
- `db/resources/Abilities/Tables/ability_set_abilities.sql` — pointer comment
- `db/resources/Abilities/Seed/ability_set_abilities.sql` — two new rows,
  comment block rewritten
- `db/resources/Abilities/Seed/ability_sets.sql` — comment block rewritten
- `crates/services/src/cell/service/npc_ai/ability_select.rs`
- `crates/services/src/cell/service/npc_ai/mod.rs`
- `crates/services/src/cell/service/mod.rs`
- `crates/services/src/cell/spawner/tests/harset/{mod,ability_sets,templates,factions}.rs`

Session 2 adds:

- `crates/services/src/cell/combat/threat/aggro.rs` — `NPC_MELEE_RANGE`
- `crates/services/src/cell/combat/threat/mod.rs`,
  `crates/services/src/cell/combat/mod.rs` — re-export chain
- `crates/services/src/cell/service/npc_ai/ability_select.rs` —
  `effective_max_range`, `choose_npc_ability_within_reach`
- `crates/services/src/cell/service/npc_ai/fight.rs` — distance before the
  pick, call the reach-aware selector
- `crates/services/src/cell/service/npc_ai/mod.rs` — test re-export
- `crates/services/src/cell/service/tests/npc_ai/{mod,melee_reach}.rs`
- `crates/services/src/cell/spawner/tests/harset/{mod,ability_sets}.rs`
- `docs/gameplay/npc-ai.md`, `docs/analysis/harset-rebuild/work-packets.md`

New:

- `docs/analysis/harset-rebuild/worknotes/H09.md` (this file)
- `crates/services/src/cell/service/tests/npc_ai/melee_reach.rs`

`Cargo.lock` picked up an unrelated churn from a concurrent build in the
shared lane and was restored with `git checkout Cargo.lock`; it is not part of
this packet.

### Three false comment blocks retired

H11 documented the one-row cap as load-bearing in three places, all of which
became false:

1. `db/resources/Abilities/Seed/ability_sets.sql` header
2. `db/resources/Abilities/Seed/ability_set_abilities.sql` header
3. the doc comment on
   `harset_ability_sets_resolve_to_an_ability_that_can_animate`, which said in
   as many words that "a future packet that tries to add variety discovers the
   constraint here rather than in a failed DB reload"

All three now describe the composite key and point at this worknote. H11's
`STAFF_AUTO_ATTACK` / `RIBBON_AUTO_ATTACK` constants ("the single member of
set 4/5") were re-documented alongside.

## Guards

Live-DB guards (TESTING.md type 3), all `require_db_or_skip!`, in
`crates/services/src/cell/spawner/tests/harset/`.

### New

| Test | What it pins |
|---|---|
| `ability_set_primary_key_is_the_composite_pair` | `pg_get_constraintdef` is `PRIMARY KEY (ability_set_id, ability_id)`; set 4 holds two distinct abilities (the **accept** half); re-inserting `(4, 710)` fails with SQLSTATE **`23505`** on constraint **`ability_set_abilities_pkey`** (the **reject** half); the count is still 2 afterwards. |
| `multi_ability_set_reaches_the_chooser_and_every_member_is_selectable` | A three-row sentinel set survives `ability_set_abilities` → `load_spawn_templates`'s `array_agg` → `SpawnRecord.ability_ids` → `spawn_npc_from_record` → the NPC's bucket → `choose_npc_ability`, and the selector reaches **every** member as cooldowns stack, then returns `None` when all three cool. |
| `seeded_harset_sets_never_select_their_melee_half_at_range` (session 2) | The melee gate against the **seed**, not a fixture: `load_ability_defs` plus real templates 200 (set 4) and 210 (set 5) out of Postgres, spawned, driven through the production `choose_npc_ability_within_reach`. Asserts `is_ranged` and the `max_range = 0` sentinel on all four abilities first, then the pick at 20 m, at 2 m, and with the ranged half cooling. |

### New — unit (session 2)

Seven guards in `cell/service/tests/npc_ai/melee_reach.rs`, a new sibling of
`selector.rs` under the existing `npc_ai` test split.

| Test | What it pins |
|---|---|
| `ranged_half_wins_when_the_target_is_beyond_melee_reach` | Set 5's shape at 20 m returns 712, with a control assertion that the *unfiltered* walk really does return 711 — so the test cannot pass because 711 happened to be unavailable. |
| `melee_half_wins_when_the_target_is_inside_melee_reach` | The same set at 2 m returns 711. Deliberately **not** a revert guard: it is the bidirectional control proving the gate filters by distance rather than blacklisting melee. |
| `out_of_reach_falls_back_to_the_melee_pick_rather_than_holding_fire` | Requirement (c) at the selector level — `Some(710)`, never `None`. |
| `single_ranged_ability_sets_are_unchanged_by_the_reach_filter` | Requirement (e) — filtered and unfiltered selection are `assert_eq!`-identical for a one-ranged-ability set. Also not a revert guard, by design. |
| `melee_only_npc_closes_the_distance_instead_of_swinging_from_range` | Requirement (c) end to end: no fire at 20 m, the movement arm ran (asserted through its `no_path` log — the Castle fixture has no navmesh, so `find_path` returns `None` and no waypoints are written), the stationary arm did not, and the same ability **does** fire once the target is at 2 m. |
| `pinned_npc_uses_its_ranged_half_rather_than_miming_a_swing` | Requirement (d) first half: the sentry fires 712 and 711 is left off cooldown. |
| `pinned_melee_only_npc_holds_fire_instead_of_swinging_at_air` | Requirement (d) second half: no fire, the `stationary_holds` log is emitted, and the NPC's yaw snaps to −π/2 (target due west, so a merely-zeroed yaw cannot pass). |

Two deliberate choices in the reject probe, both from the
`database-persistence` round. The assertion is on the SQLSTATE and the
constraint *name*, not a bare `is_err()` — `is_err()` would also pass on a
`23503` FK violation, which would make the test pass for a reason unrelated to
the key. And nothing is cleaned up because nothing is written: the duplicate
INSERT fails, and each `sqlx` statement against `&PgPool` runs in its own
implicit transaction, so the rejection poisons no later statement. (The probe
must *not* be wrapped in `pool.begin()` — inside an explicit transaction every
following statement would fail `25P02`.)

Three choices in the sentinel test:

- **A sentinel set, not set 4 padded to three rows.** Padding a production set
  would put a third ability on every live Harset Jaffa and change their attack
  rate a second time.
- **Rows inserted out of ascending order (712, 584, 710).** Inserting them
  ascending would let the test pass even if someone deleted the
  `sort_unstable()` in `choose_npc_ability`, because physical row order would
  already be the answer. This is the difference between a guard and a
  happy-path test.
- **Real ability ids (584/710/712), not invented sentinels.**
  `ability_set_abilities_ability_id_fkey` is `ON DELETE/UPDATE RESTRICT`, so
  unlike `SENTINEL_SPAWN_ID` an ability id cannot be conjured. The sentinel
  *set* id (`0x7000_6200`) and *template* id (`0x7000_6300`) reference nothing
  and are free. Cleanup deletes by exact id, child-before-parent (template,
  then `ability_set_abilities`, then `ability_sets`), and runs before the
  inserts too so an aborted run cannot wedge the next one. Insert → load →
  delete → assert, so a failing assertion cannot leak rows.

### H11 guards updated — and why each one had to be

Only where H11 pinned the one-row limitation, per the packet.

| Test | Change | Why it was forced |
|---|---|---|
| `harset_ability_sets_resolve_to_an_ability_that_can_animate` → `..._to_abilities_that_can_animate` | asserted `abilities == vec![one]`; now asserts the full membership from a `HARSET_ABILITY_SETS` const, and checks `event_set_id IS NOT NULL` on **every** member rather than the single one | the assertion was literally "exactly one row per set" |
| `harset_templates_resolve_abilities_and_respawn_through_the_spawn_loader` | `probe.ability_ids` expectation `[584]` → `[584, 710]` | the loader now returns both; the failure message now also distinguishes "empty bucket" from "loader collapsed the set" |
| `every_harset_mob_template_carries_a_non_default_ability_set` | bare `LEFT JOIN` replaced with the same correlated `array_agg` the three production loaders use; per-row `Option<i32>` checks became `Vec<i32>` checks | **H11 predicted this exact break**: its comment said the one-row-per-template count held "because `ability_set_abilities` has `PRIMARY KEY (ability_set_id)` … if that key is ever widened, this count starts failing for a reason that has nothing to do with the seed rows." The join now fans out 24 → 48. A bonus: the guard now checks the shape the runtime actually consumes. |
| `praxis_jaffa_guard_templates_use_the_staff_ability_set` | same `LEFT JOIN` → `array_agg` swap; `assert_eq!(ability_id, Some(584))` → `assert!(ability_ids.contains(&584))` | `fetch_one` over a fanned-out join silently takes whichever row came back first, making the assertion depend on physical row order — it would not have *failed*, which is worse |

Not touched: every other H11 guard (respawn, loot sweep, name allocation,
appearance, `name_id` resolution, Petbe's columns, the talk-vs-kill pairs).
None of them read `ability_set_abilities`.

## Session 2: re-verification of session 1's claims

Session 1 ended at a token limit, so none of its claims could be taken on
trust. `content/harset-wave2` was merged into `harset/H09` first (clean, no
conflicts; the base had advanced by Castle CA05–CA10, `main` through #680, and
packet H51's `face_target` / `note_outcome` work in `fight.rs`), then the whole
evidence sweep was re-run against a database freshly loaded from
`db/database.sql`.

| Claim | Verdict |
|---|---|
| The seed loads end to end with the widened key | **Confirmed.** `reload-db.sh` drops and recreates `sgw_harset_h09` from `db/database.sql`; 250 content chains, exit 0. That load *is* the fresh-load proof — the old key would have aborted it at the second row. |
| `ability_set_abilities_pkey` is `PRIMARY KEY (ability_set_id, ability_id)` | **Confirmed** via `pg_get_constraintdef`. |
| Sets 1/2/3 unchanged, 4 → `{584,710}`, 5 → `{711,712}` | **Confirmed** exactly. |
| No other seed file inserts a second row the widened key changes the meaning of | **Confirmed.** All 7 `INSERT INTO ability_set_abilities` statements in `db/` live in the one seed file; the other files that name the table are its DDL, the two FKs and the PK. |
| Every Castle and Cellblock set still loads with the membership it had | **Confirmed.** Set usage after the merge is 1 → templates {15, 219}, 2 → {4}, 3 → {24, 212-223}, 4 → {159, 160, 200-209, 221}, 5 → {210, 211}. Sets 1, 2 and 3 are the Castle/SGC sets and still hold one ability each. |
| `entity_templates_template_id_seq` `setval` is still 248 | **Confirmed** in both the seed file and the loaded sequence. |
| The 13 H09 live-DB guards pass | **Confirmed on the post-merge tree before any session-2 edit:** 13 run, 13 passed, 0 skipped. So session 1's Rust and seed really were built and working, not just described. |

Two session-1 numbers were **corrected** while re-deriving the melee case:

- The stationary count is **15 spawn rows, not thirteen** — H51's worknote and
  the H13 row both say thirteen. Twelve of the fifteen use ability set 4, one
  uses set 2, and two have no set.
- **No set-5 template is stationary.** The coordinator's hand-off described a
  "pinned Goa'uld" as the sharp case; templates 210 and 211 are the only set-5
  templates and neither has a stationary spawn row. The defect for set 5 is
  real but different: 711 sorts first, so a *mobile* ribbon NPC opened with a
  melee swing at whatever range it was at. Both are fixed by the same gate.

## Validation

Live DB: `postgres://w-testing:w-testing@localhost:5433/sgw_harset_h09` (this
worktree's own database, reloaded wholesale from `db/database.sql` by
`reload-db.sh`; session 1 used the shared `sgw_harset`).

### Statements applied by hand to `sgw_harset`

Wrapped in an explicit transaction on the `database-persistence` round —
`DROP CONSTRAINT` and `ADD CONSTRAINT` as separate autocommit statements can
half-apply and leave the shared DB with **no primary key at all** if the
second fails.

```sql
BEGIN;

SET LOCAL lock_timeout = '5s';

ALTER TABLE resources.ability_set_abilities
    DROP CONSTRAINT IF EXISTS ability_set_abilities_pkey;

ALTER TABLE resources.ability_set_abilities
    ADD CONSTRAINT ability_set_abilities_pkey
    PRIMARY KEY (ability_set_id, ability_id);

INSERT INTO resources.ability_set_abilities (ability_set_id, ability_id)
VALUES (4, 710), (5, 711);

COMMIT;
```

`lock_timeout` so an `ACCESS EXCLUSIVE` request fails fast instead of queueing
behind another packet's open transaction on the shared DB. `IF EXISTS` makes a
re-run safe; no `ON CONFLICT DO NOTHING` on the INSERT, because if those rows
are already present that should be visible, not silently absorbed.

Result: `BEGIN / SET / ALTER TABLE / ALTER TABLE / INSERT 0 2 / COMMIT`,
exit 0. Post-state: `PRIMARY KEY (ability_set_id, ability_id)`; sets 1/2/3
unchanged at `{579} / {221} / {559}`, set 4 `{584,710}`, set 5 `{711,712}`.

### Commands

| Command | Exit | Result |
|---|---|---|
| `psql -d sgw_harset -v ON_ERROR_STOP=1 -f h09_apply.sql` | 0 | `BEGIN / SET / ALTER TABLE / ALTER TABLE / INSERT 0 2 / COMMIT` |
| DB verification sweep | 0 | `PRIMARY KEY (ability_set_id, ability_id)`; sets 1/2/3 `{579} / {221} / {559}` unchanged, 4 `{584,710}`, 5 `{711,712}` |
| `cargo check -p cimmeria-services --all-targets` | 0 | clean |
| `cargo fmt --all -- --check` | 0 | clean |
| `cargo clippy -p cimmeria-services --all-targets -- -D warnings` | 0 | clean |
| **Fresh-load proof** — `psql -d sgw_h09_fresh -v ON_ERROR_STOP=1 -f db/database.sql` on a throwaway DB | 0 | loads end to end; post-state is the widened key and `4 → {584,710}`, `5 → {711,712}` |
| **Fresh-load revert proof** — same, with `_primary_keys.sql` temporarily narrowed back to `(ability_set_id)` | **3** | `ERROR: duplicate key value violates unique constraint "ability_set_abilities_pkey"` at `resources/Abilities/Seed/ability_set_abilities.sql:49`. The schema change is load-bearing, not cosmetic. |
| **Baseline** — `cargo nextest run --profile=ci-live-db -p cimmeria-services --lib -E 'test(cell::spawner::tests::harset)'` against `sgw_harset` | 0 | **13 tests run: 13 passed, 2140 skipped** |
| **Revert proof** — same command after `h09_revert.sql` | 100 | **13 tests run: 9 passed, 4 failed** |
| **Restore + re-verify** — same command after re-applying `h09_apply.sql` | 0 | **13 tests run: 13 passed**; key and all five sets back to the baseline state; zero sentinel rows left in `ability_sets`, `entity_templates` or `spawnlist` |
| `tools/lint-md.sh` (filtered to this worknote) | 0 | clean |
| Client UAT | Not run | M1 owns it — the ~2x attack-rate change is what to watch |

Both throwaway databases (`sgw_h09_fresh`, `sgw_h09_revert`) were dropped
afterwards. `sgw_harset` was never reloaded wholesale.

### Revert proof

`h09_revert.sql` restores the exact pre-H09 database state: it deletes rows
`(4, 710)` and `(5, 711)` and then narrows the key back to `(ability_set_id)`.
**Statement order is load-bearing** — narrowing first would fail on the very
duplicates it is meant to forbid and roll the transaction back, leaving a
state that is not pre-H09 and a proof that means nothing.

```text
##### 3. BASELINE
     Summary [   2.484s] 13 tests run: 13 passed, 2140 skipped
ok: no self-skip line in baseline

##### 4. REVERT (drop the H09 rows, narrow the key back)
     Summary [   0.889s] 13 tests run: 9 passed, 4 failed, 2140 skipped

##### 5. RESTORE
     Summary [   1.399s] 13 tests run: 13 passed, 2140 skipped
```

The four that fail are exactly the four H09-relevant guards, and each fails on
the assertion it was written for rather than incidentally:

| Test | Failure on revert |
|---|---|
| `ability_set_primary_key_is_the_composite_pair` | `left: Some("PRIMARY KEY (ability_set_id)")` vs `right: Some("PRIMARY KEY (ability_set_id, ability_id)")` |
| `harset_ability_sets_resolve_to_abilities_that_can_animate` | `left: [584]` vs `right: [584, 710]` |
| `harset_templates_resolve_abilities_and_respawn_through_the_spawn_loader` | `left: [584]` vs `right: [584, 710]` |
| `multi_ability_set_reaches_the_chooser_and_every_member_is_selectable` | the sentinel set's second insert hits `duplicate key value violates unique constraint "ability_set_abilities_pkey"` |

The nine that keep passing are the H11 guards this packet only reshaped —
respawn, loot, names, appearance, factions, the Praxis pair. They are not H09
guards and correctly do not track H09's rows.

Every run is grepped for `skipping live-DB test`, because `require_db_or_skip!`
turns an unreachable database into a self-skip that still reports `ok` — a
green run against the wrong `DATABASE_URL` would prove nothing. No skip line
appeared in any of the three runs.

### Session 2 revert proofs

The melee gate has two independent arms, so each was reverted on its own
rather than the whole commit at once.

**Arm 1 — the `is_ranged` branch of `effective_max_range`, deleted:**

| Suite | Result |
|---|---|
| `npc_ai::melee_reach` + `npc_ai::selector` + `npc_ai::ability_range` + `npc_ai::stationary_facing` | **24 run: 20 passed, 4 failed** |
| `harset::ability_sets` (live DB) | **5 run: 4 passed, 1 failed** |

The four unit failures are `ranged_half_wins_…`,
`melee_only_npc_closes_the_distance_…`,
`pinned_npc_uses_its_ranged_half_…` and
`pinned_melee_only_npc_holds_fire_…`, each on the assertion it was written
for. The live-DB failure is `seeded_harset_sets_never_select_their_melee_half_at_range`
with `set 5: left Some(711), right Some(712)` — the shipped defect, named.
Every pre-existing `npc_ai` selector, range and facing test kept passing, which
is what proves the new guards track this change and not something incidental.

**Arm 2 — the `.or_else(choose_npc_ability)` fallback, deleted:**

| Suite | Result |
|---|---|
| `npc_ai::melee_reach` + `npc_ai::selector` | **12 run: 9 passed, 3 failed** |

`out_of_reach_falls_back_…`, `melee_only_npc_closes_the_distance_…` and
`pinned_melee_only_npc_holds_fire_…`. The last is the interesting one: without
the fallback the selector returns `None`, `ability_ranges(None)` reports 30 m,
`in_range` is true, and the tick falls through to `no_ability` instead of
`stationary_holds` — so requirement (d) fails for a reason that has nothing to
do with the melee branch. That is exactly why the two arms are proven
separately.

Two of the seven unit guards survive both reverts on purpose:
`melee_half_wins_when_the_target_is_inside_melee_reach` (the bidirectional
control) and `single_ranged_ability_sets_are_unchanged_by_the_reach_filter`
(the requirement-(e) no-change guard). Neither is a regression guard and
neither is described as one.

## Advisor findings and resolutions

### `database-persistence` — key change and seed shape

| Finding | Resolution |
|---|---|
| **The packet names the wrong file.** `Tables/ability_set_abilities.sql` holds no constraint; every resources PK is in `_primary_keys.sql`. | Verified and accepted. Widen landed in `_primary_keys.sql`; the `Tables/` file gets a pointer comment. Recorded as a packet-text correction. |
| No FK targets this table's PK — both FKs on it are outbound, and `entity_templates_ability_set_id_fkey` already references `ability_sets`. The drop is not blocked and nothing cascades. | Verified independently in `_foreign_keys.sql`. My concern about repointing a FK was unfounded. |
| No extra index. The composite PK's leading column serves the only read pattern as a prefix scan, and the second column makes it covering and pre-sorted. | Accepted. Noted the one real consequence: the planner loses the "at most one row per `ability_set_id`" row estimate. Irrelevant at 7 rows. |
| **Wrap the ALTER in a transaction** — `DROP` then `ADD` as separate autocommits can leave the shared DB with no PK. Add `lock_timeout`; `DROP ... USING INDEX` is ceremony at this size. | Accepted in full; the applied script is above. |
| Keep one `INSERT` per row — the whole tree is `pg_dump --inserts` style and a multi-`VALUES` statement would be the only one in `db/resources/`. | Accepted; style kept. |
| Assert SQLSTATE `23505` + constraint name, not `is_err()`, which would also pass on a `23503`. Keep each probe on `&pool`, never inside `pool.begin()` (`25P02`). Sentinel needs a parent `ability_sets` row, children deleted first. | All accepted; this shaped both new tests. |
| **H09 breaks two existing tests**, and a third comment block carries the same false narrative. | Correct, and it undercounted — four H11 assertions needed changing across three files. Table above. |
| Sort order is counterintuitive for set 5 (711 melee sorts before 712 ranged). | Verified; recorded as a known consequence rather than fixed. Reordering is a chooser change. |
| Incidental: the child seed loads at line 273 before the parent `ability_sets` at 274, and only works because FKs are applied at 348. | Noted, not H09's to fix. |

### `combat-systems-advisor` — set composition and the chooser

| Finding | Resolution |
|---|---|
| **Ship both pairs.** The melee-at-30m artefact is canon, not a regression: python's `classifyHostileAbility` never range-gated the pick (`# TODO: Check distance, LOS`). | Accepted; citation verified directly in `deprecated/python/cell/SGWMob.py:227`. |
| **I had the risk ordering backwards.** 710, 711 *and* 712 all carry `event_set_id = 300`, so 711 and 712 are animation-identical on the wire — set 5 is the *safe* pair. Set 4 is the one with a real visual difference, and there the ranged ability sorts first. | Accepted; the seed comment and this note were rewritten to say so. |
| The multi-row shape is original intent: `AbilitySet.py:17-20` builds `defs[set_id].abilities[ability.id]` (a dict keyed on ability id) and `EntityTemplate.py:51-58` loops every ability in a set to validate it against the template's weapon monikers — both pointless over a one-element set. The PK is an import artefact. | **Verified independently** in both files; quoted in the `_primary_keys.sql` comment. |
| The 2x attack-rate doubling is real (no GCD, ~2s tick). Ship anyway — 1x was the bug's artefact, not a tuning decision. | Accepted; recorded above and flagged for M1 UAT. |
| The original picked `usable[0]` out of a CPython 2 dict, whose hash-slot order happened to put the *ranged* ability first in both sets. Our `sort_unstable()` reproduces that for staff and inverts it for ribbon. Explicitly: **don't fix it in H09**, it's a chooser change. | Accepted. Flagged as a known consequence. The advisor rated this an inference from CPython dict internals, not a pcap, and noted the original devs left `# TODO: Which ability should we use? Use some weighting/rand here.` directly above `usable[0]` — so lowest-id is a defensible Cimmeria convention, not a divergence from a contract. |
| Caveat stated plainly: `items_event_sets` is the *player* weapon-binding table; mobs resolve through `ability_sets`. The EntityTemplate moniker loop proves sets held weapon-bound abilities, not that they held *both halves* of a pair. | Recorded as a reconstruction, not confirmed canon. Integration request 4. |
| `choose_npc_ability` has no hostile-type / TCM filter, unlike python's, which rejected Heal/Buff/Undefined and non-`TCM_Single` before partitioning. Harmless now (all four are hostile single-target AAs); a landmine for the first author who puts a self-buff in a mob set. | Not fixed — out of scope. Integration request 1. |
| `AbilityManager::first_known_ability()` returns an arbitrary element of a `HashSet` and its doc comment advertises it "for NPCs using their default attack". Zero production callers today; harmless only while every set had one entry. | Not fixed — outside owned paths. Integration request 2. |
| The "three-bucket" model is two buckets in Rust: python called `launchReloadAbility()` when `usable` was empty and `ammo` was not. Rust has no NPC reload path. | Confirmed irrelevant here (all four are `required_ammo = 0`). Integration request 3. |
| Use non-adjacent ids inserted out of order in the sentinel, or the test passes without the sort. | Accepted — this is the best single point in the round. Adapted: ids must be **real** (FK RESTRICT), so the out-of-order insert is 712, 584, 710. |

### `testing-validation-engineer` — post-diff review

<!-- TVE -->

## Integration requests

1. **`choose_npc_ability` needs python's pre-filter.** The original rejected
   `ABILITY_TYPE_Heal`, `ABILITY_TYPE_Buff`, `ABILITY_TYPE_Undefined` and
   anything whose `targetCollectionMethod != TCM_Single` *before* the
   three-bucket partition. Cimmeria's selector takes the lowest off-cooldown
   id unconditionally. Inert today because every NPC set holds only
   single-target hostile auto-attacks — but H09 is the packet that makes
   multi-ability sets possible, so the first content author who drops a
   self-buff or an AoE into a mob set will have it fired at the player as an
   attack. Owner: whoever owns `cell/service/npc_ai/`.
2. **Remove or rename `AbilityManager::first_known_ability()`**
   (`crates/entity/src/abilities/manager.rs:140-143`). It returns
   `self.known_abilities.iter().next()` over a `HashSet`, so the result is
   arbitrary and unstable across runs, and its doc comment recommends it "for
   NPCs using their default attack" — exactly the use case that is now wrong.
   Zero production callers today (tests only), so this is a cheap cleanup
   before someone takes the doc comment at its word.
3. **`decision_outcome = "no_ability"` log text is inaccurate.**
   `npc_ai/fight.rs` says "all cooling or needs-ammo", but Rust has no
   needs-ammo bucket and no NPC reload path. One-line message fix. Left alone
   in session 2 to keep the melee-gate diff reviewable.
4. **Confirm the ranged+melee pairing against mob evidence.** The pairing is
   reconstructed from `items_event_sets`, which is the *player* weapon-binding
   table. It is well-motivated (the `EntityTemplate` moniker loop proves sets
   held weapon-bound abilities) but not proven for mobs. A mob-combat pcap or
   a finding in `docs/reverse-engineering/findings/weapon-ammo-pipeline.md`
   would settle whether a 2009 mob set held both halves. Not blocking.
5. **`crates/services/src/cell/service/mod.rs` is outside this packet's owned
   paths** and carries a one-token change (`mod npc_ai;` →
   `pub(in crate::cell) mod npc_ai;`). It is required for the loader-to-chooser
   guard to compile, is documented in place, and grants nothing outside
   `crate::cell`. Flagging for the coordinator rather than assuming consent.
   If it is vetoed, the alternative is to move
   `multi_ability_set_reaches_the_chooser_and_every_member_is_selectable` into
   `cell/service/tests/npc_ai/selector.rs` — which is also outside the owned
   list, and pulls live-DB and seed knowledge into a module that is otherwise
   pure in-memory fixtures.
6. **Event set 300 is shared** by staff-melee (710), ribbon-melee (711) and
   ribbon-ranged (712). Three weapon-specific animations would not normally
   share one event set; this smells like a generic fallback. Worth a look from
   whoever owns the sequence tables — it may mean ribbon NPCs have no
   distinct ranged animation at all.
7. **H11's worknote needs a correction breadcrumb.** Its integration request 2
   ("widen `ability_set_abilities_pkey`") is now done, and its statement that
   "no NPC can have more than one ability" is stale. Not edited here — H11.md
   is another packet's worknote.
8. **`handle_use_ability` has its own melee-blind range check.**
   `cell/abilities/use_ability/handle.rs:239-247` resolves `max_range` as
   `def.max_range > 0 ? def.max_range : 30.0`, with no `is_ranged` branch —
   the same defect the AI selector now avoids, one layer down. It is inert
   for NPCs because the AI gate never asks it to fire melee out of reach, but
   it is the gate the **player** melee path goes through, so a player can
   land a melee auto-attack from 30 m. Fixing it is a client-visible change
   to player combat and needs its own packet; it was deliberately not
   folded in here.
9. **Sets 1 and 3 could take their melee halves now.** Open question 3 below
   became cheaper once the reach gate landed: the reason to hesitate was
   that a melee half fires at range, and it no longer does. Set 1 (pistol
   579) pairs with 708 Pistol Melee AA and set 3 (SMG 559) with 595
   Automatic Weapon Melee AA, both `event_set_id = 300`. Still a different
   ledger — those sets are shared with Castle and SGC templates.

## Open questions

1. **Is the ~2x attack-rate increase on sets 4 and 5 acceptable?** It follows
   from giving each set its real membership and there is no GCD to absorb it.
   The alternative is to leave the sets at one ability, which would make the
   whole packet a no-op. Needs M1 UAT rather than a desk decision.
2. ~~**Set 5's melee-first pick order.**~~ **Resolved in session 2.** The
   reach gate makes 712 the pick at every distance beyond 3 m, so 711 no
   longer takes most of the ribbon NPC's attacks and no longer shifts them
   onto melee accuracy/defence in `calculate_qr`. 711 stays in the set and
   wins inside 3 m, which is what a ribbon device at arm's length should do.
   No reordering and no dropped row.
3. **Should sets 1, 2 and 3 get their melee halves too?** Set 1 (pistol 579)
   would pair with 708 Pistol Melee AA and set 3 (SMG 559) with 595 Automatic
   Weapon Melee AA, both with `event_set_id = 300`. Not done here because
   those sets are shared with Castle and SGC templates and this packet's scope
   is sets 4 and 5. Same change, different ledger.
4. **Templates 34/35 (SGC Jaffa)** still fall back to `NPC_DEFAULT_ABILITY`
   despite carrying the staff mesh — H11's integration request 6, still open,
   and now a two-ability fix rather than one.
