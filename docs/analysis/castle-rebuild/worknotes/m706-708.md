# m706-708 Worknotes (packets CA08 + CA09)

> Type: reference. Audience: Castle rebuild coordinator.
> Companions: [README.md](../README.md), [work-packets.md](../work-packets.md), [audit.md](../audit.md), [ca10.md](ca10.md).

## Contract

- **Packets:** CA08 — mission 706 "Power Behind the Throne" (steps 2411, 2412).
  CA09 — mission 708 "Secure the Stargate" (steps 2415, 2416, 2417, 2418, 4462,
  4469). One worker, one branch, one seed file.
- **Decisions in force:**
  - **D-CA08** — step state is the possession proof for the Control Crystal, not
    a `HasItem` check. The cell has no inventory view; the granting chain
    advances the step in the same action list. Divergence: dropping or selling
    the crystal does not regress the step.
  - **D-CA09** — Livewire for the DHD repair, labelled provisional in the seed.
    The original minigame id is unrecovered.
  - **D-CA10** — the dial and the crossing are native (CA10); mission 708 hooks
    them through the two content triggers, it does not drive them.
  - **D-CA13** — archetype split: Jaffa is archetype 8; everything else takes
    the Tau'ri branch, so the seed writes `neq 8` rather than enumerating.
  - **D-CA15** — shared world. No zone-wide mutation of a mission actor unless
    it is acceptable for every player at once. This is the decision that drove
    the one deliberate deviation from the packet text; see
    [Design decisions](#design-decisions).
  - **D-CA17 (coordinator seed split)** — new file
    `db/resources/Content/Seed/castle_706_708_chains.sql`, chains 1321-1340 for
    CA08 and 1341-1380 for CA09.
- **Base sha:** `a9d0fad7` (branch `castle/m706-708-throne-stargate`, worktree
  `.claude/worktrees/castle-m706`). The four commits were originally authored on
  `castle/ca10-stargate-events` @ `9b322812` and rebased onto `main` after CA10
  squash-merged as `a9d0fad7` (PR #663) — see
  [Rebase onto main](#rebase-onto-main-2026-09-18). No CA10 file was modified.
- **Owned paths:**
  - `db/resources/Content/Seed/castle_706_708_chains.sql` (new)
  - `crates/services/src/cell/content/chain_replay_tests/mission_706.rs` (new)
  - `crates/services/src/cell/content/chain_replay_tests/mission_708/` (new —
    `mod.rs`, `diagnosis.rs`, `crystal.rs`, `report.rs`, `stargate.rs`,
    `restore.rs`)
  - `docs/analysis/castle-rebuild/worknotes/m706-708.md` (this file)
- **Touched but not owned (coordinator merges):**
  - `db/database.sql` — one `\ir` line after the `castle_cellblock_chains.sql`
    line. `castle_701_chains.sql` and `castle_702_704_chains.sql` land on
    adjacent lines from sibling workers.
  - `crates/services/src/cell/content/chain_replay_tests/mod.rs` — two `mod`
    lines.
  - `docs/content/mission-chains.md` — two subsections appended to the Castle
    (Persistent Zone) section, immediately before `### SGC_W1`.
- **Read set:** the CA08 and CA09 packet rows, "Worker Input And Ownership",
  "Common Acceptance", README decisions D-CA08/09/10/13/15, audit "Engine Facts"
  and the "Missions 702-708" table; `castle_cellblock_chains.sql` chains
  1016/1017, 1031-1034, 1087, 1097-1099, 1106-1111;
  `crates/content-engine/src/loader/{trigger,action,condition}.rs` and
  `loader/mod.rs:88-215`; `crates/content-engine/src/chain/mod.rs:275-325`;
  `crates/content-engine/src/conditions.rs:221-245`;
  `crates/services/src/cell/content/executor/{mod,mission,inventory,dialog}.rs`
  and `executor/world/mod.rs:19-64`;
  `crates/services/src/cell/content/event_dispatch/{dialog,interaction,lifecycle,region,stargate,mission}.rs`;
  `crates/services/src/cell/content/mission_context.rs`;
  `crates/services/src/cell/missions/progression.rs`;
  `crates/services/src/cell/cell_methods/player/interaction/{dialog,interact}.rs`;
  `crates/services/src/cell/interactions/dispatch/{interact,initial_response}.rs`;
  `crates/content-engine/tests/interact_tag_linter.rs`;
  `crates/services/src/cell/content/chain_replay_tests/{mission_688,grant_xp,sgc_w1_move_entity,stargate_triggers}.rs`;
  seeds `mission_steps.sql`, `mission_objectives.sql`, `dialogs.sql`,
  `dialog_screens.sql`, `dialog_screen_buttons.sql`, `dialog_set_maps.sql`,
  `items.sql`, `spawnlist.sql`, `entity_templates.sql`, `point_sets.sql`,
  `point_set_points.sql`.

## Evidence And Hypotheses

Every chain in this packet is `RECONSTRUCTION`. There is no recovered server
script for 706 or 708: `deprecated/python/cell/spaces/Castle.py` stops at
mission 701 and never names 702, 703, 704, 706 or 708. What is original data and
is reproduced rather than invented: the step, objective and task rows; the
dialog ids and their screens; the dialog-set map rows; the region, spawn and
item rows. The wiring — which trigger drives which step, in what order — is the
reconstruction. The objective text carries most of the signal: objectives 2794
and 2795 literally read "(Option #1) Force a guard to surrender…" and
"(Option #2) Use the Access Panel on the Goa'uld Throne…", and 2798/2799 name
the NID officers and Warden Muelbach as the crystal sources.

### Four engine facts that changed the design

**1. Button-less dialogs do fire `dialog_choice`, with `button_id = -1`.**
Not one of 2584, 2586, 5003, 5004, 5008, 5009, 5010 or 5011 has a row in
`dialog_screen_buttons.sql` — checked by `screen_id`, the third column, not by
`dialog_id`. This looked like it would soft-lock steps 2415 and 2417. It does
not. The mission-systems advisor resolved it from the client: the SGW dialog
window is CEGUI + Lua and ships uncooked at
`…\SGWGame\Content\UI\Core\Dialog\`. `Dialog.lua:48-51` wires
`selectActiveDialogChoice` only to the buttons in `DialogMod.dialogButtonMap`,
so a zero-button dialog has no click path — but Done / Decline / the window's X
route to `onDialogDoneClicked` → `discardAvailableDialog` (`Dialog.lua:64-67,
83-85`), whose native implementation (`FUN_00ad86c0` → `FUN_00d249c0`) walks the
dialog's screens accumulating a button count and, **iff the total is zero**,
sends `Event_NetOut_DialogButtonChoice` with `ButtonId = 0xFFFFFFFF`. A dialog
that HAS buttons sends nothing on close. That is exactly why `Castle.py` could
key `dialog.choice::2574` and `::2575` — both also button-less — and why shipped
chains 1020/1021 key on 2300/5020. Cimmeria reads the id as a plain `i32`
(`interaction/dialog.rs:21-22`) and the #479 gate keys only on the open dialog,
so `-1` is handled.

**Hard rule that falls out: never add a button to 5003, 5004, 5008 or 5009.**
Doing so stops the close path emitting and silently soft-locks steps 2415 and
2417. Recorded in the seed header as engine fact (6).

**2. `fire_dialog_choice` does not populate `archetype`.**
`event_dispatch/dialog.rs:87-93` sets `dialog_id`, `button_id` and mission
context — nothing else. `Condition::Archetype` reads a missing key as `-1`
(`conditions.rs:225-230`), so on a `dialog_choice` chain `archetype eq 8` is
permanently false (branch dead) and **`archetype neq 8` is permanently true — a
guard that reads correct and fails open.** Confirmed against the existing seed:
every archetype-gated chain that ships today (1011/1012, 1051/1052, 1098/1099)
triggers on `enter_region` or `interact_tag`, both of which populate it. No
shipped chain puts an archetype condition on a dialog chain.

**3. The #479 open-dialog gate makes dialog-id discrimination
server-authoritative — but only because nothing binds 5008/5009 as a dialog
set.** `handle_dialog_button_choice` (`interaction/dialog.rs:36-49`) rejects a
choice unless `open_dialog_id` matches, and the pin is set only by
`send_dialog_display`. The server-authority pass correctly pointed out that the
gate proves *display*, not *entitlement*, and that `initialResponse`
(`interactions/dispatch/initial_response.rs:33-43`) lets a client name any
`dialog_set_map_id` sitting in its own `available_interactions`, unscoped by
template. That path is closed here because `available_interactions` is written
in exactly two places — `executor/dialog.rs`'s `add_dialog_set` and a GM console
command — and this packet never binds set-656 rows 5850/5851. The only way to
display 5008 is chain 1352, which is archetype-gated on the interact side.
`report.rs::archetype_conditions_are_never_placed_on_the_dialog_halves` pins
both halves of that argument against the seed.

**4. `advance_step` cannot auto-complete a mission; `complete_objective` can.**
`advance_step` force-completes the leaving step's objectives by calling
`MissionInstance::complete_objective` directly (`progression.rs:57-66`),
bypassing the module-level `all_required_complete` check at `:176-183`. So
"complete the one objective we want ticked, then advance" is safe. It is safe on
step 2417 only because all three of that step's objectives are
`is_optional = false` (`mission_objectives.sql:6631/6633/6635`): ticking 5185
leaves 5184 and 5186 active. Flip either to optional and completing 5185 ends
mission 708 three steps early. The same latent hazard exists on 2415 (survives
only because hidden objective 2796 is required) and 2416 (2797 required).
`report.rs::reporting_in_advances_the_step_without_completing_the_mission`
executes the chain against a fixture carrying the real optional flags and
asserts the mission is still `MISSION_ACTIVE` afterwards, so a seed-data flip
fails a test rather than shipping.

## Design decisions

### Deviation from the packet text: the zero-baseline rule (no mission cue is cleared)

Packet CA08 says chain 1322 should `set_interaction_type Castle_AccessPanel ~
1073741824`, and CA09 says the 2415 routes should "clear both". **Both clears
are omitted, deliberately.** The reasoning, with evidence:

- `set_interaction_type` mutates the SHARED `CellEntity.interaction_type_flags`
  and broadcasts to every witness (`executor/world/mod.rs:19-64`). There is no
  per-player interaction state short of a dialog-set bind — design gate GCA1.
- Template 147 (the Access Panel) ships `interaction_type = 0`
  (`entity_templates.sql:25`), so the glow is the ONLY bit that makes the panel
  clickable. Clearing it renders it scenery for the entire zone.
- Two different missions use the same prop at overlapping times: 706 step 2412
  (chain 1322) and 708 step 2415 route B (chain 1344). A player finishing 706
  would strip the panel from a player mid-2415, and vice versa, until one of
  them relogged into chain 1323 or 1361.
- The clear-then-reaccept ordering has a worse failure: `accept_mission` returns
  early when the offer guard refuses (already active / completed at repeat cap /
  failed non-repeatable / entity missing — `executor/mission.rs:64-72`) and
  **skips `fire_mission_accepted` entirely**. On that path chain 1341 never
  runs, and the panel stays dark for the whole zone permanently.

Setting a bit is harmless to bystanders (an extra cue); clearing one can strip
another player's only affordance.

**Widened after the TVE review of PR #668 (blocker 3).** The first draft applied
that reasoning only to the panel and still cleared the NPC `!` cues, on the
stated grounds that "a stale `!` on Marsh is worse than the cross-player
flicker". That justification silently assumed the NPCs had a non-zero baseline.
They do not:

| Tag | Template | `entity_templates.interaction_type` | Cue cleared now? |
|---|---|---|---|
| `Castle_DHD` | 162 | `16` (`INT_Dhd`) | **Yes** — chain 1357 |
| `Castle_AccessPanel` | 147 | `0` | No |
| `Castle_ColMarsh` | 10 | `0` | No — was chain 1353, row removed |
| `Castle_Mohkatan` | 54 | `0` | No — was chain 1355, row removed |
| `Castle_SurrenderGuard` | CA05 (PR #667) | unknown | No — was chains 1343/1345, rows removed |

The rule is now mechanical rather than a judgement call, because
`entity_templates.interaction_type` **is** the spawn-time value of the runtime
bitfield (`space_manager/spawn.rs:127`): *clear a mission cue only if the
target's template baseline is non-zero.* The DHD keeps `INT_Dhd` after chain
1357 clears `INT_MinigameLivewire`, so it stays clickable; Marsh, Moh'katan and
the panel would go to literally `0`.

Why that matters: `EInteractionNotificationType` is the bitfield driving the
client's right-click cursor and context menu
(`entity/src/interaction_flags.rs:1-9`). Nothing server-side re-gates it —
`handle_interact` reads the template's `NpcInteractionType` and
`available_interactions`, never `interaction_type_flags`
(`interactions/dispatch/interact.rs:93-131`), and `fire_interact_tag` runs on
ANY interact with a tagged entity
(`cell_methods/player/interaction/interact.rs:218-238`). So the breakage is
client-side and no chain-replay test can see it. That asymmetry — provable
downside, unprovable upside — is what settles it: two Tau'ri on step 2417, the
first reports in, the second can no longer click Marsh.

Cost of the rule: a stale cue over an NPC or prop the player has finished with.
The `player_loaded` restore chains 1361-1365 are still required regardless,
because `interaction_type_flags` is in-memory only and a server restart drops
every bit.

**`Castle_SurrenderGuard` is decided in advance**: apply this rule when CA05
(PR #667) seeds its template rather than re-deciding. If it ships non-zero, the
clear in chains 1343/1345 may be restored.

**Out-of-scope finding for the coordinator.** Cellblock chains 1053/1054 clear
`!`-class bit 8388608 off `Preparation_ColMarsh`, which is spawn 7 on
**template 10** — the same template as `Castle_ColMarsh` (`spawnlist.sql:7` vs
`:181`). That is the identical zero-baseline hazard, unexamined there. Not
fixed in this PR; worth its own packet, and it is not evidence that the clear
is safe.

Flagged for the coordinator as the one place this packet does not follow its
brief literally.

### Where the archetype gate lives

On the interact chains (1352 / 1354 / 1350 / 1351 / 1358 / 1359 / 1362 / 1363),
never on the `dialog_choice` chains (1343 / 1345 / 1353 / 1355). See engine
facts 2 and 3 above. The dial chains 1358/1359 CAN carry it because
`fire_stargate_dialed` populates `archetype` (`event_dispatch/stargate.rs:96-101`).

**The gate is on the dialog, not on the cue** (corrected after the TVE review of
PR #668, should-fix 4). The first draft claimed that because only one of chains
1350/1351 resolves per death, "a Human never sees a `!` over Moh'katan". Only the
first half is true. `entity_dead_tag` builds its context from the KILLER
(`event_dispatch/lifecycle.rs:64-81`, including `archetype_id`), so exactly one
cue chain resolves — but the bit it then sets is written to the shared entity and
fanned out to every witness, so a Jaffa's kill does light Moh'katan for nearby
Tau'ri. CA09's "never expose the other faction's" is satisfied on the DIALOG
(chains 1352/1354 carry the archetype condition, so a Tau'ri who walks up to a
lit Moh'katan gets nothing) and not on the cue. The cue is advisory; the dialog
is authoritative. `report_cue.rs::crystal_death_marks_exactly_one_report_npc_by_archetype`
tests resolve-level exclusivity, which is the property that actually holds, and
its assertion messages now say so. Per-player cues need design gate GCA1.

### Step 2417 uses `complete_objective` + `advance_step`, never `complete_mission`

Both report objectives are required, so `complete_objective` alone can never
finish step 2417 — the untaken branch would pin the mission open forever. The
chain completes only its own objective and lets `advance_step` force-complete
the other two. Never complete both report objectives: with 5184 also done that
would satisfy `all_required_complete` and end 708 at step 2417.

### Step 2412 and step 4469 use `complete_mission`, not `complete_objective`

`complete_objective`'s auto-complete branch sends `onMissionUpdate` with the
`MISSION_ACTIVE` status byte (`progression.rs:200`, with the giveaway comment
`// Status sent as "completed" removal`). `complete_mission_direct`
(`progression.rs:226-309`) emits the correct sequence: `onObjectiveUpdate`
(COMPLETED) per objective, `onStepUpdate` (COMPLETED), then `onMissionUpdate`
with `STATUS_COMPLETED`. Packet CA08 already called for this on 706; the same
reasoning is applied to 708's chain 1360.

### The crystal single-grant guard, and its one precondition

The step gate is the guard. Each source chain advances out of 2416 in the same
action list it grants in; `advance_step` mutates `current_step_id`
synchronously in memory (`progression.rs:76`) before returning; the cell service
is a single task owning `SpaceManager` by value, so nothing interleaves; and
`fire_entity_death` cannot re-fire for one corpse (`kill_credit.rs:56-69`
short-circuits on the `was_alive_before` snapshot). A respawned officer is the
SAME entity with the SAME tag (`ticks/npc_respawn/mod.rs:116-165`), so the step
gate is the only thing standing between a re-kill and a second crystal — and it
holds.

**Precondition: every action row must carry `delay_ms = 0`.**
`execute_actions` QUEUES rather than runs any action with `delay_ms > 0`
(`executor/mod.rs:96-120`); a deferred `advance_step` would leave 2416 active
across the delay window and turn four officers plus a warden plus respawn timers
into an unbounded crystal faucet. Every action row in the file is 0, and both
test modules assert it per chain via `assert_no_deferred_actions`.

### 2416's report cue is two multi-trigger chains, not eight

Chains 1350/1351 each carry four `entity_dead_tag` trigger rows (the precedent
is chain 1087) so the archetype gate is written once per faction instead of once
per corpse. Conditions for every matching chain are evaluated against one
pre-action context snapshot and the action lists are then concatenated
(`chain/mod.rs:288-325`), so 1350's `step_status 2416 eq active` still passes
even though 1346-1349 advance to 2417 earlier in the same batch. The replay test
registers every expansion via `load_chain_expansions_for_test` — using
`load_single_chain_for_test` would register only the first trigger row and mask
drift in the other three.

### Chain 1357's `add_dialog_set 3073` and chain 1365 are seeded but inert today

`spawner/dialogs.rs:45` still drops every `dialog_id IS NULL` row at load, so
binding map row 3073 is a warn and a no-op (`executor/dialog.rs:172-177`) until
packet CA02 widens `DialogSetMapEntry.dialog_id` to `Option<i32>`. Seeded now
because the packet asks for it, because the coordinator's CA02 wire evidence
says a flag-only bind is legal on the wire, and because it costs nothing when
inert. Worth recording: **the bind is a TOPIC ("Dial Harset"), not the dial
capability.** Template 162 already ships `interaction_type = 16` (`INT_Dhd`,
`entity_templates.sql:131`) and `space_manager/spawn.rs` copies the template
column onto the entity, so the DHD is dialable from spawn for everyone and the
dial is NOT gated on step 4462. Chains 1358/1359's `step_status 4462 eq active`
gate the mission ADVANCE, which is all mission 708 needs.

### Restore coverage

One `player_loaded Castle` chain per step that owns a cue: 2415 (1361), 2417
(1362 Tau'ri / 1363 Jaffa), 2418 (1364), 4462 (1365), plus 706's step 2412
(1323). Steps 2416 and 4469 own no cue — 1350/1351 paint at the 2416 → 2417
transition, and the resulting 2417 state is covered by 1362/1363.

## Commands run

All through the campaign lane wrapper. Build-lane v1 for the first two rows
(shared `sgw`), v2 for the last (own `sgw_castle_m706`).

| Command | Result |
|---|---|
| `lane.sh cargo fmt --all` | rc 0 |
| `lane.sh cargo check -p cimmeria-services -p cimmeria-content-engine --all-targets` | rc 0, zero warnings |
| `lane.sh cargo test -p cimmeria-content-engine --test interact_tag_linter` | rc 0 — **6 passed, 0 failed**. Covers both rules: every `interact_tag` tag in the new file has a `set_interaction_type` somewhere in it, and `Castle.ThroneRoom` byte-matches `point_sets.name`. |
| `lane.sh cargo clippy -p cimmeria-services -p cimmeria-content-engine --all-targets -- -D warnings` | rc 0 |
| `live-db-test.sh chain_replay_tests::mission_70` (guards broken, see below) | rc 100 — **40 run, 37 passed, 3 failed, 0 skipped** |
| `live-db-test.sh chain_replay_tests::mission_70` (seed restored) | rc 0 — **40 run, 40 passed, 0 skipped** |

Zero self-skips on both live-DB runs: all 40 tests executed, so
`require_db_or_skip!` found its `DATABASE_URL` and a green result means
something. The 2,031 "skipped" in the nextest summary are the rest of the
`cimmeria-services` suite filtered out by the name filter, not skips.

File sizes are re-tabulated in the TVE-review section below; the pre-review
figures recorded here were `mission_706.rs` 386, `mission_708/crystal.rs` 475,
`stargate.rs` 428 (this row read 423, a drift the review caught as nit 10),
`report.rs` 397, `diagnosis.rs` 359, `mod.rs` 208, `restore.rs` 201. The seed
is data and exempt.

## Rebase Onto Main (2026-09-18)

CA10 squash-merged as `a9d0fad7` (PR #663), so replaying this branch with the
CA10 commits still under it would have re-applied CA10's pre-squash history.
The four commits were moved onto `main` on their own.

The base sha in the original brief, `0d3c9147`, is **not** an ancestor of this
branch — CA10 rewrote that commit into `e47bda1a` during its own review round.
The real last CA10 commit under this work is `9b322812`:

| Command | Result |
|---|---|
| `git rebase --onto origin/main 9b322812` | rc 0 after two conflict resolutions |
| `lane.sh cargo fmt --all` | rc 0, no reformat |
| `lane.sh cargo clippy -p cimmeria-services -p cimmeria-content-engine --all-targets -- -D warnings` | rc 0 |
| `lane.sh cargo test -p cimmeria-content-engine --test interact_tag_linter` | rc 0 — **6 passed, 0 failed** |
| `live-db-test.sh chain_replay_tests::mission_70` (guards deleted) | rc 100 — **88 run, 85 passed, 3 failed, 0 skipped** |
| `live-db-test.sh chain_replay_tests::mission_70` (seed restored) | rc 0 — **88 run, 88 passed, 0 skipped** |

The filter now matches 88 tests rather than the 40 recorded above, because
mission 701's 48 guards landed on `main` in PR #659 and share the
`mission_70` prefix. All 40 of this packet's tests are inside that 88.

Both conflicts were add/add on a single line and were resolved by keeping both
sides:

- `db/database.sql` — `castle_701_chains.sql` (PR #659) and
  `castle_706_708_chains.sql` now sit on consecutive `\ir` lines after
  `castle_cellblock_chains.sql`.
- `crates/services/src/cell/content/chain_replay_tests/mod.rs` — `mod
  mission_701;` plus `mod mission_706;` / `mod mission_708;`.

Three small corrections came with the rebase:

1. `mission_708/stargate.rs` destructures `Action::StartMinigame { .., .. }`
   instead of exhaustively. CA04 (PR #652) adds a loader-defaulted
   `difficulty` field to that variant, and an exhaustive arm here would break
   on it without adding any signal — the arm asserts `minigame_type` and
   `on_victory_chains`, the two fields chain 1356 actually owns.
2. The seed's step-4462/4469 comment cited CA10 by branch name; it now cites
   PR #663. The code references it makes were re-checked against merged `main`
   and all still hold: `loader/trigger.rs:58-63` for the two trigger names,
   `event_dispatch/stargate.rs:96-101` for the `archetype` population, and
   `target_entity: None` at `stargate.rs:105` for the `display_dialog`
   fallback caveat on 5010/5011.
3. `cell::gate_travel::on_stargate_passage` survived the squash intact
   (`gate_travel/mod.rs:211`), so the "crossing fires before the teardown"
   premise chain 1360 depends on is unchanged.

Chain-id collisions were re-checked against every seed file now on `main`:
this packet holds 1321-1323 and 1341-1365; Cellblock holds 1001-1175, mission
701 holds 1201-1243, effects 2001-2025, SGC_W1 3001-3028, consumables 4001.
No overlap. Actor tags do not overlap either — 701 uses only
`Castle_Coppleman` and `Castle_SgtGerschon`.

The `thiserror 2.0.19` → `2.0.20` `Cargo.lock` delta present in the worktree
at resume was discarded: `main`'s lock is internally inconsistent and the drift
is not this packet's.

## Regression proof

Done once, both missions in a single run, by deleting the two guard
condition rows from the committed seed and reloading the database from it.

Guard 1 (mission 706) — chain 1322 loses
`mission_status 708 eq not_active`. That row is both the mandatory
accept gate and the discriminator against chain 1344, which triggers on the
same `Castle_AccessPanel` tag for mission 708.

Guard 2 (mission 708) — chains 1346-1349 lose
`step_status 708 2416 eq active`, leaving only `mission_status 708 eq
active`. That row is the entire single-grant guard for the Control Crystal.

Result: **40 run, 37 passed, 3 failed**, and the three failures are exactly
the tests that exist for those rows:

```text
FAIL mission_706::chain_1322_does_not_refire_once_708_is_active
FAIL mission_708::crystal::crystal_is_granted_only_once_across_two_officer_deaths
FAIL mission_708::crystal::no_crystal_source_fires_outside_step_2416
     panicked at crystal.rs:270: chain 1346 must not resolve while the
     player is on step 2415
```

Nothing else moved — in particular
`each_crystal_source_grants_2790_completes_its_objective_and_advances` and
`crystal_death_marks_exactly_one_report_npc_by_archetype` still passed,
which is what makes the three failures attributable to the deleted rows
rather than to a broken fixture. The seed was then restored with
`git checkout --` and the suite re-run green.

**Re-proved after the rebase onto `main`** (2026-09-18), this time with an
in-place `DELETE` against `sgw_castle_m706` rather than a second reload —
`DELETE 1` for chain 1322's `mission_status` row and `DELETE 4` for 1346-1349's
`step_status` rows, then the replay run inside the same lane hold. Result:
**88 run, 85 passed, 3 failed** — the same three test names, at the same three
assertion sites (`mission_706.rs:311`, `crystal.rs:361`, `crystal.rs:270`).
Mission 701's 48 guards, now in the same filter, all passed throughout, which
is additional evidence the failures track the deleted rows. The database was
then reloaded from the committed seed and the run came back **88/88, 0
skipped**.

## TVE Review Of PR #668 (2026-09-18)

The testing-validation-engineer static review returned **yellow**: 3 blockers,
6 should-fix, 4 nits, zero tests flagged as theatre. Every item was applied;
nothing was declined. Test count 40 -> 45.

### Blockers

1. **Seed comment claimed executed guards for four chains when only one had
   one.** `execute_actions` appeared at five sites, none of them chains 1343,
   1345 or 1355. Fixed by adding the guards rather than narrowing the claim:
   `diagnosis.rs::{guard_route,panel_route}_advances_without_completing_the_mission`
   (new `stage_player_on_step_2415` fixture with 2796 required+hidden and
   2794/2795 optional) and
   `report.rs::the_jaffa_report_advances_without_completing_the_mission`. The
   existing 2417 body was generalised into
   `assert_report_advances_without_completing` so both factions run it. The seed
   comment now names the five test functions **and** states what they do not
   catch -- see Known gaps 11.
2. **The crystal fixture never asserted `mission.status`.** Step 2416's only
   required objective is 2797; had it been flipped to optional,
   `complete_objective 2798` would have completed mission 708 at 2416 and the
   test would still have passed, because `advance_step` runs afterwards in the
   same list and sets `current_step_id` to 2417 regardless. One assertion added
   after the first `execute_actions` in `crystal.rs`.
3. **The D-CA15 analysis that spared the Access Panel had never been run on the
   two report NPCs.** This is the substantive finding. Resolved by generalising
   the panel's one-off exemption into the mechanical **zero-baseline rule** (see
   Design decisions). Four `set_interaction_type ~` rows removed (chains 1343,
   1345, 1353, 1355); the DHD's clear in chain 1357 stays, because template 162
   ships a baseline of `16` (`INT_Dhd`) and therefore never drops to flags 0.

### Should-fix

- **4. Cue exclusivity was overclaimed** in the seed and inherited by a test doc
  comment: only the RESOLVE is exclusive, the bit is zone-wide. Reworded in
  three places (seed, `report_cue.rs` module docs and assertion messages,
  worknote "Where the archetype gate lives").
- **5. Chains 1346-1349's `mission_status 708 eq active` row was unfalsifiable.**
  Added the orphaned-step case -- step 2416 active while the mission is
  `not_active` -- to `no_crystal_source_fires_outside_step_2416`. That pairing
  cannot occur in production (`populate_mission_context` always writes both
  keys), which is why only a fixture can pin the row.
- **6. Engine fact 3 was load-bearing and untested.** New
  `report_cue.rs::the_crystal_grant_and_its_report_cue_resolve_in_one_batch`
  registers chain 1346 and all four expansions of 1350 in one engine and
  asserts the COMBINED action list carries both. It earns the
  highest-consequence label the review gave it: combined with finding 3, a
  per-chain re-evaluation would leave the killer on step 2417 facing a report
  NPC at flags 0.
- **7. 1322-vs-1344 exclusivity was never tested with both chains registered.**
  New `mission_706.rs::one_panel_click_never_resolves_both_706_and_708_chains`
  fires one `Castle_AccessPanel` interact through a two-chain engine in all
  three real states and asserts the other chain's list is empty each time.
- **8. `mission-chains.md` prose contradicted the sections this PR adds.** Fixed;
  the stale *count* in the section header is still deferred to the last
  integrator by design (Integration edit 3).
- **9. Kill credit is single-player** -- added as Known gap 10 plus a UAT M4 line.

### Nits

- **10.** File-size table recorded `stargate.rs` at 423; it was 428. Re-tabulated
  below.
- **11.** `gate_ctx("Castle", ...)` made the wrong-destination negatives
  non-discriminating, because `gate_ctx` also stamps `world_name = "Castle"`.
  Now `OTHER_WORLD = "Abydos"`, a third name, so the negative also pins which
  key the matcher reads.
- **12.** `assert_no_deferred_actions` was duplicated verbatim in `mission_706.rs` and
  `mission_708/mod.rs` with drifting panic text. Hoisted to
  `chain_replay_tests/mod.rs` as a single `#[cfg(test)]` helper.
- **13.** Nothing owed -- `work-packets.md` is untouched by this branch and the ledger
  flip is the coordinator's edit.

### File split

`crystal.rs` reached 524 lines once blocker 2 and should-fix 5 landed, past the
500 soft cap. Split along the seam the review's own framing implies: the grant
chains 1346-1349 are a server-authority problem (exactly one item, ever) while
the cue chains 1350/1351 are a presentation problem. The two cue tests moved to
a new `mission_708/report_cue.rs` together with should-fix 6's new test, and
`SOURCES` moved up to `mission_708/mod.rs` because both files enumerate it.

Post-review sizes, all under the soft cap: `mission_706.rs` 469,
`mission_708/diagnosis.rs` 498, `crystal.rs` 447, `report.rs` 440,
`stargate.rs` 435, `report_cue.rs` 204, `restore.rs` 201, `mod.rs` 199.

### Commands (TVE round)

| Command | Result |
|---|---|
| `git rebase origin/main` (onto `5c354b1e`, CA04 / PR #652) | rc 0, no conflicts |
| `lane.sh cargo fmt --all` then `cargo fmt --all -- --check` | rc 0 |
| `lane.sh cargo clippy -p cimmeria-services -p cimmeria-content-engine --all-targets -- -D warnings` | rc 0 |
| `lane.sh cargo test -p cimmeria-content-engine --test interact_tag_linter` | rc 0 -- **6 passed, 0 failed**. Still green after the four `~` rows were removed: the linter requires a `set_interaction_type` per `interact_tag` tag, and every `\|` row remains. |
| `live-db-test.sh chain_replay_tests::mission_70` (guards broken) | rc 100 -- **93 run, 80 passed, 13 failed, 0 skipped** |
| `live-db-test.sh chain_replay_tests::mission_70` (seed restored) | rc 0 -- **93 run, 93 passed, 0 skipped** |

The filter matches 93 because mission 701's 48 guards share the `mission_70`
prefix; 45 of the 93 belong to this packet. The "2,109 skipped" in the nextest
summary is name-filter exclusion, not `require_db_or_skip!` bailouts --
`live-db-test.sh` prints the `DATABASE_URL` it exports
(`postgres://.../sgw_castle_m706`, 179 content chains loaded).

### Regression proof (TVE round)

Eight reverts applied in place against `sgw_castle_m706` -- no second reload --
with the whole filter run inside the same lane hold.

| Revert | Guard that must fail | Failed? |
|---|---|---|
| A: chain 1343 also completes required 2796 | `diagnosis::guard_route_advances_without_completing_the_mission` | yes, `diagnosis.rs:123` |
| B: chain 1345 also completes required 2796 | `diagnosis::panel_route_advances_without_completing_the_mission` | yes, `diagnosis.rs:123` |
| C: chain 1346 also completes required 2797 | `crystal::crystal_is_granted_only_once_across_two_officer_deaths` | yes, `crystal.rs:389` -- the NEW assertion |
| D: chain 1355 also completes 5184 + 5185 | `report::the_jaffa_report_advances_without_completing_the_mission` | yes, `report.rs:356` |
| E: delete `mission_status 708 eq active` from chains 1346-1349 | `crystal::no_crystal_source_fires_outside_step_2416` | yes |
| F: re-gate chain 1350 on step 2417 | `report_cue::the_crystal_grant_and_its_report_cue_resolve_in_one_batch` | yes, `report_cue.rs:188` |
| G: delete chain 1344's step gate | `mission_706::one_panel_click_never_resolves_both_706_and_708_chains` | yes, `mission_706.rs:455` |
| H: re-insert the chain 1353/1355 cue clears | `report::each_report_completes_only_its_own_objective_and_arms_the_dhd` | yes |

Every failure carried its own assertion message naming the exact bug. For C:
`left: 2, right: 1` on `mission.status` -- `MISSION_COMPLETED` where
`MISSION_ACTIVE` was required -- while the `current_step_id == Some(2417)`
assertion immediately above it still passed. That is exactly the hole blocker 2
described.

**Ordering trap worth recording.** The first attempt at reverts A/B/C injected
the extra `complete_objective` at a `sort_order` AFTER the chain's
`advance_step`. It then resolved against the NEXT step's objective list,
early-returned at `progression.rs:155-157`, and never reached the all-required
check -- so all three guards stayed green and looked vacuous. They are not; the
revert was wrong. Any future proof of this shape must push `advance_step` to a
higher `sort_order` first. Recorded because a mis-ordered revert is
indistinguishable from a weak guard in the test output.

Non-vacuity: 80 tests stayed green through the broken run, including mission
701's 48 guards and this packet's entire restore and stargate families. Five
collateral failures are expected, because those reverts also change action
counts and trigger gates that other tests assert on:
`chain_1343_completes_2794_then_advances_to_2416`,
`chain_1345_completes_2795_then_advances_to_2416`,
`chain_1344_does_not_fire_during_mission_706`,
`report_cue::crystal_death_marks_exactly_one_report_npc_by_archetype` and
`report_cue::report_cue_chains_do_not_fire_outside_step_2416`.

## Copilot Review Of PR #668 (2026-09-18)

Five inline comments. Three are the same zero-baseline hazard the TVE review
raised as blocker 3, independently found: chains 1343/1345 on the guard
(`:393`/`:429`), chains 1353/1355 on Marsh and Moh'katan (`:651`/`:685`), and
the Access Panel's never-cleared glow (`:281`). The first two are fixed by the
zero-baseline rule; the third is the rule's deliberate consequence.

Copilot contributed one fact the TVE pass did not have: **CA05's planned
template 148 for `Castle_SurrenderGuard` also ships `interaction_type = 0`.**
That resolves the deferred decision in Integration edit 6 in advance — the
guard's clear should stay removed. The seed still routes the decision through
the rule rather than hard-coding the answer, because CA05 (PR #667) has not
landed and the template could still change before it does.

Two comments were genuine doc defects and are fixed here:

- **The Castle metadata block** read `Total scripted missions: 2 (701, 702)`,
  which both reviewers read as the zone's mission total and which the new 706 /
  708 sections contradicted. The TVE round deferred it to the last integrator to
  keep the merge surface small; with two reviewers on it that is no longer the
  right trade. Relabelled into three unambiguous fields — missions in zone (6),
  covered by recovered server script (2), ported to content chains (3) — so the
  distinction the original line was trying to make survives. Integration edit 3
  no longer defers anything.
- **Dialog 2586 was described as "reached through the 3073 topic bind".** It is
  not reached at all. No `dialog_set_maps` row anywhere carries
  `dialog_id = 2586`; row 3073 is set 656 with `interaction_flags = 16`
  (`INT_Dhd`), `topic_text = 'Dial Harset'` and `dialog_id = NULL` — a bindable
  indicator row, not a dialog. Row 5711 (set 654) genuinely does bind dialog
  2584 for mission 706, which is what made the parallel look plausible. The seed
  was already accurate (it cites 2586's text only as weak D-CA09 evidence for
  which minigame the DHD repair used); the reference doc was not. Corrected, and
  the contrast with 5711 spelled out so the next reader does not re-derive it.

## Known gaps

1. **Five tags do not exist yet.** `Castle_SurrenderGuard`,
   `Castle_BravoOfficer1..3` and `Castle_Muelbach` are contracts owed by packet
   CA05 and are absent from `spawnlist.sql` world 8. Until CA05 seeds them,
   `find_entity_by_tag` returns `None`, `set_interaction_type` debug-logs and
   no-ops (`space_manager/queries.rs:325-332`), and the four death triggers
   never fire. The replay guards exercise the chains against hand-built
   fixtures and pass regardless; **the zone does nothing for mission 708's steps
   2415 and 2416 until CA05 merges.** Make that explicit at UAT.
2. **Three Bravo officers is CA05's default N.** If CA05 seeds a different
   count, add or remove chains in the 1346-1348 block and the matching trigger
   rows on 1350/1351, and update the `SOURCES` table in `crystal.rs`.
3. **Dialogs 5010 and 5011 depend on the interaction pin.**
   `fire_stargate_dialed` stamps no `target_entity_id`, so `display_dialog`
   falls back to `last_interaction_target` (`executor/dialog.rs:54-90`). At step
   4462 the DHD has no matching interact chain (1356 requires 2418), so the
   click that opens the dial UI falls through to `handle_interact`, which pins
   the DHD (`interactions/dispatch/interact.rs:89-91`). The portrait therefore
   binds to the DHD while the screens' own speakers (261 Marsh / 2499
   Moh'katan) name the voices. If UAT shows the closing line missing, a null
   pin is the cause. The coordinator's CA17 candidate (`display_dialog` optional
   `target_tag`) is the clean fix and would let 5010/5011 bind the named actor
   instead.
4. **Item 2136's role is a guess.** It is named "Muelbach…" and carries a
   copy-pasted Ambernol description. It is granted as the identifying drop that
   distinguishes the Muelbach route, mirroring item 2135 on mission 703. Nothing
   consumes it. If UAT says it is meaningless, delete the one action row from
   chain 1349.
5. **Livewire on the DHD is provisional (D-CA09).** At step 2418 the DHD's flags
   are `16 | 256 = 272` and the client offers both a dial and a hack affordance;
   `fire_interact_tag` short-circuits `handle_interact`
   (`cell_methods/player/interaction/interact.rs:160-200`), so chain 1356 wins
   the interact while 2418 is active and the native dial path takes over once it
   is not. Workable, but it needs a client UAT.
6. **The Access Panel sits just outside its own region.** The panel is at
   x = 330.49 (`spawnlist.sql:133`); point set 2049's bounding box starts at
   x ≈ 331.35 (`point_set_points.sql:121-127`). Harmless today — chain 1321
   fires on region entry, not on proximity to the panel — but a future
   tightening of the region test would bite.
7. **`enter_region` is client-hinted.** A player who somehow accepted 706 while
   already standing in the Throne Room never fires chain 1321 and stalls on
   2411. 706 is accepted in the Communications room (CA07), so this is fine
   today; do not let a future re-order break it.
8. **Objective state does not survive a relog** — `executor/mission.rs:85,242`
   persist `active_objective_ids: vec![step_id]` and no completed objectives.
   After a relog, chain 1353's `complete_objective 5185` silently no-ops and the
   step machine still recovers, so 708 is not broken, but the client's objective
   ticks are fiction. Pre-existing and cross-mission; already filed as **#657**
   by the m702-704 packet. Not re-filed here.
9. **`advance_step` force-completes optional objectives too.** A player who took
   the surrender-guard route at 2415 will also see "(Option #2) Use the Access
   Panel" tick. Engine behaviour (`progression.rs:57-66`), not something the
   seed can suppress.
10. **Kill credit is single-player** (added after the TVE review of PR #668,
    should-fix 9). `fire_entity_death` builds its context from the killer alone
    (`event_dispatch/lifecycle.rs:64-81`), so in a group only the player who
    lands the killing blow progresses step 2416 and receives the Control
    Crystal. Other members sitting on 2416 get nothing until an officer
    respawns. Packet CA09's "any of N" scope reads as though any group member
    progresses. Engine behaviour, not something the seed can fix, and invisible
    in a solo UAT — hence the new UAT M4 line below.
11. **The executed guards do not read the objective seed.** Every
    `stage_player_on_step_*` fixture hard-codes the `is_optional` / `hidden`
    flags that `mission_objectives.sql` supplies. So the five MISSION_ACTIVE
    guards catch a chain in this file growing an extra `complete_objective`
    row, and they catch a regression in the executor's all-required check — but
    an `is_optional` flip in `mission_objectives.sql` itself would NOT fail
    them, because the fixture would keep asserting the old flags. Mirroring
    such a flip into the fixtures is a manual step. A fixture that loaded the
    objective rows from the DB would close this; it needs a shared helper that
    does not exist yet and is worth its own packet.
12. **No test type can express a zone-wide flag mutation across two players.**
    `set_interaction_type`'s blast radius is a `SpaceManager` + witness-list
    property and chain-replay (TESTING.md type 6) is single-player by
    construction. The zero-baseline rule above is therefore recorded evidence
    plus a UAT line, not a guard. A two-player `SpaceManager` fixture — stage A
    and B in one space, run chain 1353 for A, assert B's view of the target's
    `interaction_type_flags` — would be a new sub-shape of type 6 and deserves
    a TESTING.md row if the Castle campaign keeps hitting it.

### UAT additions (M4)

Both come out of the TVE review and neither is expressible as a chain-replay
test:

1. **Two Tau'ri on step 2417.** The first reports in to Col. Marsh. The second
   must still be able to click Marsh. This is the zero-baseline rule's
   observable consequence; had the clear been kept, the second player would
   find Marsh inert.
2. **Two players both on step 2416.** Only the killer receives the Control
   Crystal — confirm the second player can re-kill a respawned officer and get
   their own. (Respawn is itself unreliable in the Castle: `respawn_secs` is
   NULL on most spawns.)

## Integration edits the coordinator must make

1. **`db/database.sql`** — this branch adds
   `\ir resources/Content/Seed/castle_706_708_chains.sql` immediately after the
   `castle_cellblock_chains.sql` line. `castle_701_chains.sql` and
   `castle_702_704_chains.sql` land on adjacent lines from the sibling workers;
   the conflict is a trivial line-order resolution. Load order does not matter —
   the chain id ranges are disjoint and nothing in this file references another
   file's chain.
2. **`crates/services/src/cell/content/chain_replay_tests/mod.rs`** — two `mod`
   lines (`mission_706`, `mission_708`), alphabetically placed after
   `mission_689`. Same conflict shape as (1).
3. **`docs/content/mission-chains.md`** — two subsections appended to the Castle
   (Persistent Zone) section, inserted immediately before `### SGC_W1 (SGU
   Starting Zone)`. The sibling packets append to the same place. The
   "Missions 702-708 remain unported" prose sentence is corrected in this PR
   (TVE should-fix 8) to name 701/706/708 as ported and 702/703/704 as
   outstanding. **The section header's "Total scripted missions: 2 (701, 702)"
   count is still stale and was left alone on purpose** to keep the merge
   surface small — whoever integrates last should correct it once and note that
   702-708 are reconstruction rather than recovered script.
4. **Rebase done** — the branch now sits directly on `main` @ `a9d0fad7` (CA10
   squash-merged as PR #663). Nothing left to rebase; see
   [Rebase onto main](#rebase-onto-main-2026-09-18) for the two conflicts and
   how they were resolved.
5. **Ledger** — CA08 and CA09 to Review/Integrated. The deliberate deviation
   from the packet text is now broader than the panel: **no mission cue on a
   zero-baseline entity is cleared anywhere in this file** (chains 1343, 1345,
   1353, 1355). It wants either a ledger note or amended packet rows; it is a
   D-CA15 consequence, not an oversight. See the zero-baseline rule under
   Design decisions.
6. **`Castle_SurrenderGuard`'s clear is deferred to CA05 (PR #667), not
   dropped.** When that packet seeds the guard's template, read its
   `interaction_type`: non-zero means chains 1343/1345 may carry the
   `~ INT_AStoryMissionActive` row again; zero means they must not. The rule is
   written into the seed's engine fact 7 so the decision need not be
   re-litigated.
7. **Out-of-scope bug worth its own packet.** Cellblock chains 1053/1054 clear
   `!`-class bit 8388608 off `Preparation_ColMarsh`, which is **template 10 —
   the same template as `Castle_ColMarsh`** (`spawnlist.sql:7` vs `:181`). Same
   zero-baseline hazard, unexamined there. Restore chain 1063 repaints only on
   login, so between chains 1053 and 1066 a second player in Preparation may
   find Marsh inert. Not touched by this PR.
8. **Consider a follow-up packet** for per-player interaction cues (GCA1).
   Every `set_interaction_type` in the Castle is zone-wide. After the
   zero-baseline rule this packet no longer *clears* any shared cue, but it
   still *sets* them zone-wide, so a bystander sees cues for a step they are
   not on. Cellblock 1107 and 1053/1054 still carry the clearing form.
