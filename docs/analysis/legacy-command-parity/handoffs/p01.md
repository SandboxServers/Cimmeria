# P01 Handoff

> Type: reference. Audience: legacy-command-parity coordinator.
> Companions: [worknotes/p01.md](../worknotes/p01.md), [README.md](../README.md), [work-packets.md](../work-packets.md).

## Status: complete for the commands in scope, with one documented gap (see below)

## Changed paths

- `crates/services/src/cell/console/registry.rs` — added `ArgSpec`/`arg_specs` (per-argument
  metadata for `.help`'s `<= 3`-match detail view). 586 → 636 lines (hard cap 700, untouched).
- `crates/services/src/cell/console/query.rs` — `.help` now prints per-argument detail (required
  unbracketed / optional bracketed) when `<= 3` commands match; zero-match line now reports the
  exact legacy wording `"No command found with that name"`. 149 → ~175 lines.
- `crates/services/src/cell/console/tests.rs` — 4 new `legacy_p01_*` tests.
- `crates/services/src/base/console_authoring.rs` — added `escape_ilike_pattern` and
  `run_console_search`; `handle_console_search` now escapes `%`/`_`/`\` for literal matching and
  reports exact-vs-truncated hit counts at the 25-result boundary. 230 → ~567 lines (2 pure +
  8 live-DB `legacy_p01_*` tests included in that count).
- `docs/analysis/legacy-command-parity/worknotes/p01.md` (new)
- `docs/analysis/legacy-command-parity/handoffs/p01.md` (new, this file)

Source base: `371910eeab065f0b3b6c84eeeca54bbe97767fdd` on branch `p01-quick-wins-console`,
worktree `C:\Users\Steve\source\projects\Cimmeria-worktrees\p01`. All work is committed to that
branch in that worktree; nothing pushed, no PR opened, `legacy-command-parity` untouched, per
instructions.

Environment note: this worktree's `external/` directory (native navmesh sources, populated by
`setup.ps1`, gitignored) was absent on creation — a fresh-checkout condition CLAUDE.md documents.
I junction-linked it to the main checkout's `external/` (`New-Item -ItemType Junction ... -Target
C:\Users\Steve\source\projects\Cimmeria\external`) to unblock the `cimmeria-entity` native build
step. No source files were affected; flagging in case another worktree needs the same fix.

## Behavior implemented

### `.help`

- Unfiltered/filtered listing unchanged (sorted, substring-filtered, one-line summaries).
- **New:** when the filtered result set has `<= 3` commands, each argument is now printed as
  `    name (type): desc` (required) or `    [name] (type): desc` (optional), mirroring legacy
  `ConsoleCommands.py::help`'s detail view — but with the intended `index < min` semantics instead
  of legacy's buggy `index <= argsMin` (D02: "correct legacy bugs rather than reproducing them").
  Populated for `help` (`command`) and the search family (`name`, `name2`) — see "Gaps" below for
  the other ~59 commands.
- **New:** zero-match line is now the exact legacy wording, `"No command found with that name"`
  (was `"help: no command matched."`).

### `.searchitem` / `.searchmission` / `.searchtemplate`

- `%`, `_`, `\` in the GM's query text now match literally (via `ESCAPE '\'` + input escaping),
  matching legacy's plain Python substring search instead of acting as SQL wildcards.
- Case-insensitive and multi-word search preserved/verified (two-token join already happened
  cell-side in `query.rs::search`'s `args.join(" ")`, unchanged).
- Truncation is no longer silent: exactly 25 matches reports the true count; 26+ matches reports
  25 with an explicit "(results truncated, refine your search)" suffix.
- The three Rust-only names (`seedconfirm`/`seedpending`/`seedcancel`) and all 11 `path_*` names
  are untouched; `command_names_are_unique` and `every_spec_is_dispatched` still pass.

## Remaining gap

`arg_specs(name)` in `registry.rs` returns an empty slice for every registered command except
`help`/`searchitem`/`searchmission`/`searchtemplate`. `.help`'s `<= 3`-match detail view is
therefore correct in mechanism but incomplete in data: filtering to `<= 3` other commands (e.g.
`.help delspawn`) shows their one-line summary but no per-argument breakdown. Building accurate
per-arg metadata for those ~59 commands requires reading their owning legacy family files
(`Entity.py`, `Net.py`, `Player.py`, `Mission.py`, `Crafting.py`), which are outside this packet's
assigned read set and outside P01's command scope. This matches one of P01's listed stop
conditions verbatim ("the legacy per-argument help semantics for `argsMin` are ambiguous against
actual `Command.args` shapes you can't resolve from the Python alone"); rather than stopping the
whole packet over it, I scoped the verified mechanism+data down to the 4 in-scope commands and
left the rest as an explicit, non-fabricated gap (see worknote "Deferred / gaps"). Recommend each
future packet add its command(s)' `ArgSpec` row(s) to `registry.rs` when it restores that
command's real behavior.

## Validation — exact commands, exit codes, counts

All run from `C:\Users\Steve\source\projects\Cimmeria-worktrees\p01`.

```text
$ cargo check -p cimmeria-services
Finished `dev` profile [unoptimized + debuginfo] target(s) in 6.69s
Exit code: 0
```

```text
$ cargo test -p cimmeria-services --lib legacy_p01_ -- --test-threads=1
running 12 tests
test base::console_authoring::tests::legacy_p01_search_backslash_is_literal ... ok
test base::console_authoring::tests::legacy_p01_search_is_case_insensitive_and_multi_word ... ok
test base::console_authoring::tests::legacy_p01_search_literal_percent_applies_to_mission_and_template_kinds ... ok
test base::console_authoring::tests::legacy_p01_search_no_matches_returns_empty_untruncated ... ok
test base::console_authoring::tests::legacy_p01_search_percent_is_literal_not_wildcard ... ok
test base::console_authoring::tests::legacy_p01_search_truncates_at_25_and_reports_exact_count_at_boundary ... ok
test base::console_authoring::tests::legacy_p01_search_underscore_is_literal_not_wildcard ... ok
test base::console_authoring::tests::legacy_p01_search_unknown_kind_returns_empty_label ... ok
test cell::console::tests::legacy_p01_help_own_optional_arg_is_bracketed_not_required ... ok
test cell::console::tests::legacy_p01_help_shows_argument_detail_at_exactly_3_matches ... ok
test cell::console::tests::legacy_p01_help_unfiltered_omits_argument_detail ... ok
test cell::console::tests::legacy_p01_help_zero_match_reports_legacy_wording ... ok
test result: ok. 12 passed; 0 failed; 0 ignored; 0 measured; 1689 filtered out
Exit code: 0
```

Ran without `DATABASE_URL` first to confirm the 8 live-DB tests self-skip cleanly (verified via
`--nocapture`: each printed `skipping live-DB test (DATABASE_URL not set)`), then re-ran below
against the live DB.

```text
$ DATABASE_URL=postgres://w-testing:w-testing@localhost:5433/sgw \
  cargo nextest run --profile=ci-live-db -p cimmeria-services --lib legacy_p01_
Starting 12 tests across 1 binary (1689 tests skipped)
        PASS [   0.464s] legacy_p01_search_backslash_is_literal
        PASS [   0.154s] legacy_p01_search_is_case_insensitive_and_multi_word
        PASS [   0.517s] legacy_p01_search_literal_percent_applies_to_mission_and_template_kinds
        PASS [   0.159s] legacy_p01_search_no_matches_returns_empty_untruncated
        PASS [   0.106s] legacy_p01_search_percent_is_literal_not_wildcard
        PASS [   0.232s] legacy_p01_search_truncates_at_25_and_reports_exact_count_at_boundary
        PASS [   0.317s] legacy_p01_search_underscore_is_literal_not_wildcard
        PASS [   0.038s] legacy_p01_search_unknown_kind_returns_empty_label
        PASS [   0.013s] legacy_p01_help_own_optional_arg_is_bracketed_not_required
        PASS [   0.012s] legacy_p01_help_shows_argument_detail_at_exactly_3_matches
        PASS [   0.014s] legacy_p01_help_unfiltered_omits_argument_detail
        PASS [   0.017s] legacy_p01_help_zero_match_reports_legacy_wording
Summary [   2.060s] 12 tests run: 12 passed, 1689 skipped
Exit code: 0
```

Broader regression check (not requested, run to confirm no collateral breakage in the console
module — full module, not just this packet's filter):

```text
$ cargo test -p cimmeria-services --lib console:: -- --test-threads=1
test result: ok. 42 passed; 0 failed; 0 ignored; 0 measured; 1659 filtered out
Exit code: 0
```

Not run: `cargo fmt --all -- --check`, `cargo clippy`, full-workspace `cargo build`/`cargo
nextest run` (coordinator's serialized pre-PR gate per instructions), `tools/lint-md.ps1` on this
handoff/worknote pair (small, ASCII, ordinary Markdown — not run explicitly). Client UAT: **Pending
user session** (cannot run in-client UAT from this environment).

## Regression proof (each fix reverted individually, confirmed to fail, then restored)

1. **Search literal-escaping** (`escape_ilike_pattern` reverted to `input.to_string()`,
   no-op): `legacy_p01_search_percent_is_literal_not_wildcard`,
   `legacy_p01_search_underscore_is_literal_not_wildcard`, and
   `legacy_p01_search_literal_percent_applies_to_mission_and_template_kinds` all failed with the
   decoy-row assertion (`"literal '%' must NOT act as a SQL wildcard and match the decoy row"` /
   underscore equivalent). Restored; all 3 pass again.
2. **Search truncation boundary** (`.bind(SEARCH_LIMIT + 1)` reverted to `.bind(SEARCH_LIMIT)`,
   restoring the old silent-cap behavior): `legacy_p01_search_truncates_at_25_and_reports_exact_count_at_boundary`
   failed with `"26 matches must report truncated = true"`. Restored; passes again.
3. **`.help` off-by-one fix** (`i < spec.min` reverted to `i <= spec.min`, reintroducing the
   legacy bug): `legacy_p01_help_own_optional_arg_is_bracketed_not_required` and
   `legacy_p01_help_shows_argument_detail_at_exactly_3_matches` both failed (`"optional arg
   'name2' must render bracketed"` / `"help's own optional 'command' arg must render
   bracketed"`). Restored; both pass again.
4. **`.help` zero-match wording** (reverted to the old `"help: no command matched."` text):
   `legacy_p01_help_zero_match_reports_legacy_wording` failed. Restored; passes again.

All 4 regression points independently confirmed via a live `cargo test`/`cargo nextest` run
against each reverted state — not asserted from memory.

## State / persistence / client-recipient evidence

- `.help` and `.search*` are read-only; no DB writes, no runtime state mutation. Live-DB tests
  insert/query/delete sentinel rows only (base `0x7000_5000`, one slot past the highest reserved
  elsewhere in the crate — `contact_list::persistence::tests::TEST_BASE = 0x7000_4000`); cleanup
  deletes by exact sentinel id, never by range, per TESTING.md.
- Caller/subject separation: both command families operate on the caller only (`Target::None`);
  no target/subject entity is touched. Feedback is single-recipient
  (`send_gm_feedback`/`send_gm_feedback_to_client`, both already-existing single-recipient
  helpers) — no leakage to other players; not independently retested since this packet didn't
  touch the delivery mechanism, only the message content and query logic.

## Unresolved wire questions

None. `.help` and `.search*` feedback is plain-text `onPlayerCommunication` on the existing
`CHAN_FEEDBACK` channel, unchanged wire shape from before this packet.

## Reviewer

Not yet reviewed by a second party. `testing-validation-engineer` review of the regression
strategy and `documentation-writer` review of any public documentation (see below) are
recommended before integration, per work-packets.md's "Worker Input And Ownership".

## Documentation

Checked CLAUDE.md's "Required documentation for every PR" map. No existing doc documents GM
`.`-console command *behavior* in detail (the closest candidates, `docs/commands.md` and
`docs/protocol/`, are explicitly the player-facing `/slash` catalogue and wire-format references
respectively — conflating them with the GM `.`-console would be a real bug, per the instructions
for this packet). The `.`-console framework's own doc pointer,
`docs/architecture/dev-console-channel.md` (referenced from `console/mod.rs`'s module doc), was
not read or touched — whether it needs a P01-driven update is a call I'm leaving to the
coordinator/documentation-writer rather than making unilaterally, since I don't know that doc's
current scope or the coordinator's plan for updating it per-packet vs. once at a later milestone.

## Client UAT status

Pending user session (M1 Quick wins milestone, per README.md's Validation And UAT Gates table).
Not run in this session.
